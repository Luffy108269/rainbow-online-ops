# RB Online Ops — Master Tracker
> อัปเดตล่าสุด: 24 ก.ค. 2569 (เที่ยงคืน — เครื่องที่บ้าน) · Build: **B3.131 (html) / 3.112 (gs)** — deploy @132 curl ยืนยัน 3.112 แล้ว
> B3.131: ซ่อนการ์ด 🛒 Ketshop สด (OpenAPI 403 ไม่มีคีย์ — Luffy สั่งซ่อน 24 ก.ค.) โค้ดยังอยู่แค่ comment บรรทัดโชว์ · **⏰ trial หมด ~3-4 ส.ค. → ถาม Luffy ก่อนว่าลบโค้ด OpenAPI ถาวรไหม (จดใน memory Claude แล้ว)**

## 🔗 24 ก.ค. 2569 (ตี 1 — เครื่องที่บ้าน) — ไฟล์เร่งงานผูกรหัส: Ketshop 3,316 SKU เทียบคัมภีร์+รอผูก (Luffy ขอด่วน)
- **โจทย์ Luffy:** งานผูกรหัสกินเวลาจนงานอื่นไม่เดิน — ขอไฟล์ Excel ว่า SKU Ketshop ตัวไหนยังไม่ผูกในเว็บ ภายในพรุ่งนี้
- **ทำแล้ว (เสร็จคืนเดียว):** ดึงสินค้าทั้งร้านผ่าน Ketshop MCP `/product/list_all` 7 หน้า (3,316 variant · อ่านอย่างเดียว) + อ่านชีต Database ผ่าน Drive MCP (export xlsx) → จับคู่ 3 ทาง: **ผูกแล้ว 1,396 · ยังไม่ผูก 1,920** · เดาคู่อัตโนมัติจากรหัส WS ฝังใน SKU: ฝั่งรอผูก 497 เจอคู่ 79 ตัว (1 WS → หลาย variant ไซส์ ตามกติกาเดิม) · ที่เหลือ SKU ยุคเก่าชื่อไทยไม่มีเบาะแส ต้องเทียบตา
- **ไฟล์:** `A:\Claude\ผูกรหัส_Ketshop_ยังไม่ผูก_24-07-69.xlsx` (นอก repo — ข้อมูลธุรกิจห้ามเข้า git public) 4 แท็บ: อ่านก่อน / ① KS ยังไม่ผูก 1,920 (เรียงเบาะแส+สต็อกมากก่อน) / ② รอผูก 497+เบาะแส / ③ คัมภีร์ไม่มี WS 7
- **✅ Luffy รีวิวแล้ว (ตี 1):** แท็บ ① เบาะแส WS **ถูกหมดทั้ง 11 ตัว** (EA010010146-conchshell ↔ EA010010146 ฯลฯ) — เดินหน้า mass-bind กลุ่มนี้ได้ · แท็บ ② (79 ตัว) ยังไม่ได้รีวิว รอเคาะก่อนผูกหมู่
- **คิวต่อที่เสนอ:** Luffy รีวิวแท็บ ② เสร็จเมื่อไหร่ → เขียน mass-bind รอบใหม่ (แบบ massBind217) ผูกหมู่อัตโนมัติ ไม่ต้องกดมือทีละตัว · **รื้อหน้าแรก (เคาะสเปกแล้ว):** ยอดขายสดขึ้นบน+auto-load · ใกล้หมดยุบ 3 แถว · รอผูกเหลือแถบเดียว · กลุ่มสีย้ายไปหน้าสินค้า (Luffy ตอบ: ไม่ค่อยเปิด) · ตัวเลขบนหัวเปลี่ยนเป็นยอดขายวันนี้/ออเดอร์วันนี้/ใบเบิกค้าง + รีเฟรชทุกครั้งที่กลับหน้าแรก — ทำพรุ่งนี้

## 🛒 23 ก.ค. 2569 (ดึก — เครื่องที่บ้าน) — เฟสประมวลผล webhook เฟสแรก: แท็บ KsOrders + การ์ดยอดขายสด (B3.129/3.111 @131 ✓)
- **ไขปริศนา payload ครบด้วยจอจริงจาก Luffy 3 ออเดอร์:** status **1=สร้างใหม่ · 2=รอชำระเงิน (UNPAID) · 4=เสร็จสิ้น (COMPLETED)** (เลขอื่นยังไม่เจอ — เจอเมื่อไหร่เพิ่มใน `KS_STATUS_TH_`) · เงิน: `amount`=ยอดสินค้าหลังหักส่วนลด `delicharge`=ค่าส่ง `totals`=ยอดชำระ (ตรวจสมการกับ payload จริง 8 ออเดอร์ ตรงทุกใบ · amount ติดลบได้ถ้าส่วนลดเกินราคา เช่น ที่วัดไซส์) · **⚠️ `details[].price` = ยอดรวมของบรรทัด ไม่ใช่ราคา/ชิ้น** (เทียบจอ 2607001388: 11×2=22) · `channel` = marketplace จริง (shopee/lazada) · token dump อยู่เครื่องบ้านแล้ว: `apps-script/webhook-dump-url.txt` (gitignored) — Claude ดึง payload เองได้ตลอด
- **Luffy เคาะแผน ก.:** ประมวลผลแยกจากฝั่งรับ — ฝั่งรับ webhook ไม่แตะแม้แต่บรรทัดเดียว · ตัวประมวลผลรันตอนการ์ดรีเฟรช/กดเมนู (ไม่ต้องตั้ง trigger ไม่ต้อง authorize เพิ่ม)
- **ของใหม่ 3.111:** แท็บ `KsOrders` (1 แถว/ออเดอร์ · ไม่เก็บชื่อ/ที่อยู่/เบอร์ลูกค้า) · `ksOrdersProcess_` — cursor `KS_ORD_ROW` ใน Script Properties อ่านเฉพาะแถวดิบใหม่ / cursor เกิน lastRow (ลบแถวดิบ) = reset อ่านใหม่ทั้งหมด (upsert idempotent) / LockService / กรอง sku `K-*` / dedupe แถวหลังชนะ · action `ksSales` (L/A — ประมวลผลก่อนตอบเสมอ) · เมนูชีต **🛒 ประมวลผลออเดอร์ Webhook → KsOrders** (Yes=rebuild ทั้งหมด No=เฉพาะใหม่)
- **ของใหม่ B3.129:** การ์ด **🛒 ยอดขายสด (webhook)** หน้าแรก (L/A · กดรีเฟรชเอง): วันนี้/เดือนนี้ แยก "ยืนยันแล้ว" (ทุกสถานะยกเว้น 2) กับ "รอชำระ" · ยอดต่อช่องทางเดือนนี้ · ออเดอร์ล่าสุด 5 ใบพร้อมป้ายสถานะ — **ฟรีตลอด ไม่พึ่ง OpenAPI/MCP/trial**
- **ทดสอบ:** จำลอง loop ประมวลผลใน node กับ payload จริง 10 แถว → 8 ออเดอร์ dedupe/กรอง/สมการเงินถูกครบ ✓ การ์ด render ผ่าน stub บน local server :8124 ครบ (ตัวเลข 4 ช่อง/เรียงช่องทาง/ป้ายสถานะ 3 แบบ/footer) ✓ syntax+dup+onclick ✓ · deploy @131 curl ตอบ 3.111 ✓ ไม่แตะ DriveApp
- **ใช้ครั้งแรก:** เปิดเว็บ → การ์ด 🛒 ยอดขายสด → กด 🔄 รอบแรกจะไล่ย่อยแถวดิบที่ค้างทั้งหมดอัตโนมัติ · **คิวต่อ (เฟส 2):** กระจกสต็อกจากออเดอร์ (รอ mapping SKU จากไฟล์ export หลังบ้าน — คิวข้อ 3) · เจอ status ใหม่เพิ่มใน map · ตรวจ signature HMAC
- **B3.130/3.112 (เที่ยงคืน) — hotfix จากการใช้จริงรอบแรกของ Luffy:** การ์ดโชว์ออเดอร์ล่าสุดถูก (สะสม 70 ใบ) แต่วันนี้/เดือนนี้ = 0 หมด — **Sheets แปลงข้อความ order_date เป็น Date object ตอนเขียนลงชีต** อ่านกลับ String(Date)='Thu Jul 23…' เทียบ '2026-07-23' ไม่ตรง → เพิ่ม `ksDs_` format Date→'yyyy-MM-dd HH:mm:ss' ก่อนเทียบ/sort/ส่งออก ทุกจุดใน ksSales · deploy @132 ✓ · **Luffy ถาม TikTok:** รวมอัตโนมัติ — นับตามฟิลด์ channel จาก payload ช่องทางไหนเชื่อม Ketshop อยู่ก็โผล่เอง (คืนแรกเห็นแค่ shopee/lazada เพราะยังไม่มีออเดอร์ TikTok เข้า) · การ์ด 🛒 Ketshop สด (OpenAPI) ตัวบนยัง 403 ตามเดิม — Luffy กดสลับแล้วงง เสนอซ่อนจนกว่าได้คีย์ รอคำตอบ

## 📎 23 ก.ค. 2569 (ค่ำ — เครื่องที่บ้าน) — แก้ Expense บนมือถือ: ป้ายรูปแนบหาย/กดเพิ่มรูปไม่ได้ (B3.128 / gs คงเดิม 3.110)
- **Luffy เจอบนมือถือ:** บางรายการไม่เห็นรูปแนบ + แนบรูปเพิ่มไม่ได้ · **สาเหตุ:** ตัวหนีบ 📎 อยู่ในบรรทัดชื่อรายการที่ตั้ง `overflow:hidden` — จอแคบ+ชื่อยาว = 📎 โดนตัดหลุดจอ เลยไม่มีทางเข้าหน้ารูปเลย (backend ปกติ ส่ง photos ครบ)
- **แก้:** แยก 📎 ออกมาเป็นป้ายเม็ดยาแบบ flex-shrink:0 ข้างชื่อ (ชื่อยาวตัด … ฝั่งเดียว) — โชว์เสมอทุกรายการ: มีรูป=ชมพูเข้ม / 0 รูป=เทา (ยังกดเข้าไปเพิ่มรูปได้) · tap area ใหญ่ขึ้น (padding 2px 8px)
- **ทดสอบ local server (:8124 — พอร์ต 8123 โดนแชท Re-order ใช้อยู่ แก้ launch.json แล้ว) จอ 375px:** ป้ายโชว์ครบ 3 เคส (📎2/📎1/📎0) ✓ กดเปิด modal + ช่องเพิ่มรูป (L/A) ✓ แถว 0 รูปขึ้น "ยังไม่มีรูปแนบ"+เพิ่มได้ ✓ ไม่มี h-scroll ✓ ไม่มี console error/ฟังก์ชันซ้ำ ✓
- **เกร็ดเล่าให้ Luffy แล้ว:** สารบัญชีตต้อง**คลิกเดียว**ที่ลิงก์ → การ์ดเด้ง → กดชื่อแท็บในการ์ด (ดับเบิ้ลคลิก = เข้าโหมดแก้สูตร ลิงก์ไม่ทำงาน — ไม่ใช่บั๊ก)

## 🌙 จบงาน 23 ก.ค. 2569 (เครื่องที่ทำงาน) — ทุกชั้น sync ที่ 3.110 ครบ ทำต่อจากบ้านได้เลย
- **ทุกชั้นตรงกัน:** เว็บจริง @130 = 3.110 (curl ยืนยัน) = repo B3.127 (push แล้ว) = Apps Script HEAD — **เครื่องที่บ้าน: `git pull` แล้ว `clasp pull` ใน apps-script/ ก่อนแตะ backend เสมอ** (ไฟล์ในเครื่องที่บ้านค้างที่ 3.107 · ใช้ชื่อ `รหัส.js` — Code.gs เก่าลบทิ้งได้เลย กันไฟล์ซ้ำตอน push)
- **งานวันนี้ 5 รอบ:** B3.125/3.108 รับ Ketshop Webhook · B3.126/3.109 เก็บกวาด Code.gs · @129 dump endpoint · B3.127/3.110 ตีกลับใบเบิก+กันกรอกสลับช่อง · เคาะยุทธศาสตร์ MCP = ไม่จ่าย
- **คิวทำต่อจากบ้าน:** (1) เช็คแท็บ `KsWebhook` — ออเดอร์สะสมทั้งบ่าย/เย็น ดู payload เพิ่ม (ดึงผ่าน dump endpoint ได้) (2) ตีกลับ WD-2607-038 ให้ Ticha แก้ ถ้ายังไม่ได้กด (3) Luffy export สินค้าทั้งร้านจากหลังบ้าน Ketshop → ทำ mapping+สแนปช็อต (4) Claude ดึง `/report/*` ชุดวิเคราะห์ (deadline ~3 ส.ค. ก่อน trial หมด) (5) แผนล้างทุนจม v2 รอเคาะ (6) ออกแบบเฟสประมวลผล webhook (การ์ดยอดขายสด/กระจกสต็อก) กับ Luffy

## 🔁 23 ก.ค. 2569 (เย็น รอบ 2) — ตีกลับใบเบิกที่ Store อนุมัติแล้ว + กันกรอกสลับช่อง (B3.127/3.110 @130 ✓)
- **ปัญหาจริงจากทีม:** WD-2607-038 — Ticha (Store) กรอกน้ำหนักลงช่องจำนวนชิ้น (qty=0.91!) แล้วช่อง Net จริงว่าง ตอนอนุมัติไม่มีอะไรเตือน · Luffy เคาะหลักการ: **คนตรวจไม่แก้ตัวเลขแทน — ตีกลับให้ Store แก้เอง** (แก้แทน = ผิดบ่อยขึ้นเพราะไม่ระวัง)
- **ปุ่ม 🔁 ตีกลับให้ Store** (แท็บ ✅ อนุมัติแล้ว · เฉพาะ **Assist/Luffy** — Admin ไม่มีสิทธิ์ตามที่ Luffy เคาะ): action ใหม่ `wdReject` — เหตุผลบังคับทั้ง 2 ฝั่ง → ใบถอย approved → sent (ล้าง confirmed_by/invoice_date) กลับแท็บ ⏳ ขออนุมัติ · **ไม่แตะตัวเลขรายการ/รูป/PDF** (ถูกเขียนทับตอน Store อนุมัติรอบใหม่) · ป้ายแดงเหตุผลโชว์บนการ์ดทั้งแท็บขออนุมัติ+อนุมัติแล้ว (note ขึ้นต้น 🔁 บรรทัดแรก) · LockService + audit ลง Log
- **กันพลาดต้นทาง (สำคัญกว่า):** (1) ช่องจำนวนชิ้น step=1 + เช็คตอน submit ทุกแท็บ — ทศนิยม = บล็อกทันที พร้อมบอก "ตัวเลขทศนิยมคือน้ำหนัก ให้กรอกช่อง g ชั่งจริง" + focus ช่องผิด (2) Store กดอนุมัติทั้งที่ Net จริงว่าง N รายการ → confirm ซ้ำถึงผ่าน (ลำดับ: เช็คจำนวนเต็ม → gross → รูปตราชั่ง → confirm Net ว่าง → confirm อนุมัติ)
- **ทดสอบผ่าน local server (http-server :8123) ครบ:** ปุ่มโชว์เฉพาะ assist/master ✓ admin/storefront ไม่เห็น ✓ ป้ายแดงทั้ง 2 แท็บ ✓ qty 0.91 โดนบล็อกก่อนถึง api ✓ ลำดับ photo gate คงเดิม ✓ แนบรูปจริงแล้ว Net ว่าง → confirm เตือนโผล่+ยกเลิกได้ ✓ wdRejectAsk เหตุผลว่างโดนกัน/เหตุผลจริงยิง api ถูก payload ✓ · syntax+dup+handler ครบ ✓ · ไม่แตะ DriveApp ไม่ต้อง initDrivePermission
- **⚠️ ใช้กับใบจริง:** WD-2607-038 ที่ค้างอยู่ — Assist/Luffy เปิดแท็บ ✅ อนุมัติแล้ว → กด 🔁 ตีกลับให้ Store → ใส่เหตุผล → Ticha แก้จำนวน (30→? เช็คด้วย) + Net จริง แล้วอนุมัติใหม่ · เพิ่ม config server ใน `.claude/launch.json` ชื่อ `rainbow-online-ops` (:8123) ไว้เทสต์รอบหน้า

## 🎯 23 ก.ค. 2569 (เย็น) — Luffy เคาะ: ไม่จ่าย MCP แน่นอน → แผนรีด trial 11 วัน (หมด ~3-4 ส.ค.)
- **ธง: ไม่เสียเงินทุกกรณี** — ตัดการทดสอบเขียนสต็อกทิ้ง (ไม่มีประโยชน์แล้วเพราะยังไงก็ไม่ต่อ) · เป้าเดียว: เก็บของถาวรให้ครบก่อน trial หมด
- **บทเรียนจากการลองดึงจริง:** ร้านมี 3,316 รายการ (นับราย variant) · `/product/search` แบก description ยาวมาก · `/product/list_all` เบากว่าแต่ก็ยังไม่เหมาะดึงหมดผ่านแชต → **ดัมพ์ก้อนใหญ่ใช้ปุ่ม Export หลังบ้าน Ketshop แทน (ฟรีตลอด ไม่ผูก trial)** · MCP เก็บไว้ใช้กับสิ่งที่ export ไม่ได้ = รายงานวิเคราะห์สำเร็จรูป `/report/*` (dead-stock, revenue-summary, order-channel, orders-by-time, RFM segmentation, repeat-purchase-rate, cancelled-orders, cohort) — response เล็ก คุ้มโทเคน
- **แผนเก็บเกี่ยว:** (1) Luffy export สินค้าทั้งร้านจากหลังบ้าน (Product Manager) → ส่งไฟล์ให้ Claude → สร้างแท็บ mapping SKU Ketshop↔RBO + สแนปช็อตสต็อกตั้งต้น ลงชีตถาวร (2) Claude ดึง `/report/*` ชุดวิเคราะห์เก็บเป็นไฟล์/ชีตถาวร (เสบียงแผนล้างทุนจม + benchmark ยอดขาย) (3) webhook สะสมออเดอร์ต่อเนื่องอยู่แล้ว = ข้อมูลสดหลัง trial หมด
- **หลัง trial หมด สิ่งที่เสีย:** การถามวิเคราะห์สดในแชต (ทดแทนด้วย export ไฟล์เป็นรอบ ๆ) · ฝั่งเขียน push สต็อกอัตโนมัติ (ไม่เคยเปิดใช้อยู่แล้ว — ทีมคีย์มือเหมือนเดิม)

## ✅ 23 ก.ค. 2569 (บ่าย รอบ 3) — Webhook ติดแล้ว! ออเดอร์ Shopee จริงไหลเข้าชีต + payload มี SKU/จำนวนครบ (@129)
- **เส้นทางเปิดใช้จริง:** รอบแรก Fail ทุกนัด — บั๊กที่เมนูใช้ `ScriptApp.getService().getUrl()` ซึ่งคืน URL deployment เก่าที่ตายแล้ว (404) → แก้เป็น URL /exec จริงฝังตรง ๆ (ตัวเดียวกับ index.html) → Luffy วาง URL ใหม่ → **Success ต่อเนื่อง: orderUpdate 2607001664/2607001408 (channel shopee) เข้าชีต KsWebhook ครบ** — ฝั่ง Ketshop ขึ้น Success ด้วย (ไม่ติดเรื่อง 302)
- **โครง payload จริง (วิเคราะห์แล้ว):** `{signature, type, data:{ordercode, channel/channel_name/channel_ordercode, status, packed, totals, ชื่อ-ที่อยู่-เบอร์ลูกค้า, order_date, details:[{sku, qty, price, title, properties…}]}}` — **`details[]` มี SKU+จำนวน+ราคาครบทุกชิ้น** (มีรายการหลอก K-SHOPEE/K-S-Discount = ค่าธรรมเนียม/ส่วนลด ต้องกรองตอนประมวลผล) · `signature` 44 ตัว base64 น่าจะ HMAC-SHA256 (คีย์น่าจะเป็น X-KET-API-KEY d63… — เฟสหน้าใช้ตรวจความแท้ได้)
- **นัยสำคัญต่อการตัดสินใจ MCP:** webhook ฟรี ให้ "ข้อมูลไหลเข้า" ครบ (ยอดขายสด/กระจกสต็อกหักตามออเดอร์/สถานะ) โดยไม่ต้องมี OpenAPI/MCP เลย → มูลค่า MCP เหลือ 2 อย่าง: อ่านตามสั่งในแชต + ฝั่งเขียน push สต็อก (ยังไม่ทดสอบ ค้าง 1/3)
- **เครื่องมือใหม่:** doGet เพิ่มโหมด dump — `/exec?src=ks&dump=1&tk=<KS_WH_TOKEN>&n=N` (สูงสุด 10 แถว) ให้ Claude ดึง payload ล่าสุดมาวิเคราะห์เองผ่าน curl ได้ ไม่ต้องรบกวน Luffy copy จากชีต · deploy @129 (build คง 3.109 — เพิ่ม endpoint อย่างเดียว)
- **คิวถัดไป (เฟสประมวลผล — ออกแบบกับ Luffy ก่อนทำ):** ตาราง KsOrders สรุปรายออเดอร์ + การ์ดยอดขายสดหน้าแรกจาก webhook · กระจกสต็อก (ต้อง mapping sku Ketshop↔RBO — ใช้ตรรกะเดียวกับ KsCompare) · ตรวจ signature · เคลียร์ status code ของ Ketshop (เจอ 2=?, 4=? ต้องถาม/เทียบ) · ลบแถวทดสอบ/จัดขนาดชีต KsWebhook ถ้าโตเร็ว

## 🧹 23 ก.ค. 2569 (บ่าย รอบ 2) — เก็บกวาด Code.gs จบคิวค้าง (B3.126/3.109 @128 ✓) + Webhook เปิดรอออเดอร์จริง
- **Luffy ตั้ง Webhook ฝั่ง Ketshop ครบแล้ว:** Status เปิด ✓ Marketplace เปิด ✓ (ให้ครอบคลุมออเดอร์ Shopee) URL ชี้มา /exec ของเรา ✓ events orderCreate+orderUpdate ✓ — **รอออเดอร์จริงเข้า** แล้วดู 2 จุด: แท็บ Logs webhook ฝั่ง Ketshop (302 = ปกติของ Apps Script ไม่ใช่ error) + แท็บ `KsWebhook` ในชีตเรา → ได้ payload จริงแล้วส่งคอลัมน์ raw ให้ Claude ออกแบบขั้นประมวลผล (คำถามชี้ขาด: มี SKU/จำนวนสินค้าใน payload ไหม)
- **เก็บกวาด Code.gs (คิวค้างตั้งแต่ 22 ก.ค.):** ลบ massBind217Preview/massBind217/massBind217_ + ข้อมูลฝัง MB_ALBUMS/MB_ROWS 739 แถว (~900 บรรทัด — รันจบ 21 ก.ค. ข้อมูลเข้าชีตถาวรแล้ว) · ลบ `SHEETS.Expenses` นิยามซ้ำตัวแรก (JS ใช้ตัวหลังอยู่แล้ว พฤติกรรมไม่เปลี่ยน) · ลบเมนู 📧 + `menuSendKetshopLetter` (เลิกใช้ — คุยทางแชต support แทน) · เหลือ 95 ฟังก์ชัน ไม่มีซ้ำ เมนูทุกตัวมี handler ครบ · ไฟล์เบาลงจาก ~4,230 → 3,325 บรรทัด
- **หมายเหตุเครื่องที่ทำงาน:** ไฟล์ backend ในเครื่องนี้ตอนนี้คือ `apps-script/รหัส.js` (จาก clasp pull — ชื่อตรงกับบนเว็บ) · Code.gs 3.105 เก่าลบทิ้งแล้ว (สำรองใน scratchpad ชั่วคราว) — เครื่องที่บ้านถ้าจะแก้ backend ให้ clasp pull ก่อนเสมอเหมือนกัน

## 🛒 23 ก.ค. 2569 (บ่าย — เครื่องที่ทำงาน) — คุย Ketshop support ได้ข้อสรุปใหญ่ + รับ Webhook เข้าระบบ (B3.125/3.108 @127 ✓)
- **คำตอบจาก Ketshop support (คุยทางแชต — เมล Gmail draft ไม่ต้องส่งแล้ว ค้างอยู่ใน Drafts ไว้ลบได้):**
  1. **แพ็กเกจปัจจุบันใช้ OpenAPI เต็มไม่ได้** — MCP ได้สิทธิ์ OpenAPI แค่บางส่วน → 403 ที่เจอไม่ใช่คีย์ผิด แต่แพ็กเกจไม่มีสิทธิ์ · Luffy ประเมินว่า Ketshop ไม่ปล่อย OpenAPI เต็มแน่
  2. **MCP เป็นของเสียเงิน: ~1,200 บาท/เดือน** — ตอนนี้เป็น **trial 14 วัน ใช้มาแล้ว 3 วัน หมด ~3-4 ส.ค.** ต้องตัดสินใจต่อ/ไม่ต่อก่อนนั้น
  3. **support แนะนำ Webhook สำหรับเชื่อมระบบภายนอก** → Luffy เจอหน้าตั้งค่าในหลังบ้าน (Extension → Webhook) มีในแพ็กเกจปัจจุบัน
- **ไขปริศนาคีย์ d63…:** `X-KET-API-KEY` ที่หน้า Webhook = คีย์ที่ Ketshop แนบใน header ตอนยิงมาหาเรา (ไว้ตรวจฝั่งรับ) — ไม่ใช่คีย์ OpenAPI ตั้งแต่แรก สมเหตุสมผลที่ /auth/token ตอบ 403
- **Webhook events มี 9 ตัว (ออเดอร์+ลูกค้าล้วน ไม่มี stock/product):** orderCreate · orderUpdate · pointUpdate · tagUpdate · billingAddressUpdate · orderShippingAddressUpdate · orderPackedUpdate · batchOrderUpdate · batchOrderCreate + สวิตช์ Marketplace เปิด/ปิดแยก (ต้องลองว่าครอบคลุมออเดอร์ Shopee ไหม)
- **ของใหม่ 3.108 (backend อย่างเดียว · frontend ขยับแค่ stamp):** `doPost` รับ webhook ที่ `/exec?src=ks&tk=<token>` (Apps Script อ่าน HTTP header ไม่ได้ → ตรวจ X-KET-API-KEY ไม่ได้ ใช้ token ลับใน URL แทน · token = `KS_WH_TOKEN` ใน Script Properties) → เก็บ event **ดิบ** ลงชีตใหม่ `KsWebhook` (ts/event/order_id/bytes/raw cap 45k) ยังไม่ประมวลผลใด ๆ = เฟสเรียนรู้ payload ก่อน · เมนูชีตใหม่ **🛒 ตั้งค่า Webhook Ketshop** (`menuKsWebhook`) — สร้าง token ครั้งแรก + โชว์ URL เต็มให้ copy ไปวางใน Ketshop · ไม่แตะ DriveApp ไม่ต้อง initDrivePermission
- **Deploy แล้ว:** เครื่องที่ทำงาน clasp pull ก่อน (Code.gs เครื่องนี้ค้าง 3.105 — ลบทิ้ง ใช้ `รหัส.js` จาก pull) → push → redeploy @127 → curl ตอบ 3.108 ✓ · ทดสอบยิง POST token ผิด → `bad token` ถูกต้อง ✓
- **⚠️ ขั้นตอน Luffy (webhook จะยังไม่ทำงานจนกว่าทำครบ):** (1) เปิดชีต → เมนู 💰 RB ราคา → **🛒 ตั้งค่า Webhook Ketshop** → copy URL (2) วางใน Ketshop: Extension → Webhook → Webhook URL + เลือก events orderCreate/orderUpdate + เปิด Status + บันทึก (3) ทำออเดอร์ทดสอบ/แก้ออเดอร์ 1 ตัว → ดูแท็บ `KsWebhook` ฝั่งเรา + `Logs webhook` ฝั่ง Ketshop (4) ได้ payload จริงแล้ว Claude ค่อยออกแบบขั้นประมวลผล (ยอดขายสด/กระจกสต็อกจากรายการในออเดอร์ — ขึ้นกับว่า payload มี SKU/จำนวนไหม)
- **คิวตัดสินใจ MCP (ก่อน ~3-4 ส.ค.):** ทดสอบ MCP **เขียน** สต็อก (increase +1 / decrease −1 คืน SKU ทดสอบ) — ถ้าเขียนได้ = จ่าย 1,200/เดือนได้เป้าเฟส C ครบผ่าน Claude เป็นตัวกลาง (confirm 3 ครั้งตามกฎ) · ยืนยันแล้ว 1/3 ครั้ง รอ Luffy สั่งต่อ · ระหว่าง trial: รัน KsCompare เก็บรายงานถาวร + ดึงข้อมูลประกอบแผนล้างทุนจมให้หมด

## 🧭 24 ก.ค. 2569 (กลางคืน) — Nav ใหม่ 7 ปุ่ม + F5 รอบ 3 จบ: เลิกใช้ Excel planner ได้แล้ว (B3.124/3.107 @126 ✓)
- **Luffy อนุมัติเต็มก่อนนอน** ("ทำเลย เทออนุมัติให้จบเลย") — สเปกเคาะแล้ว: 1) ยุบ nav 10→7 ตามที่ Luffy เสนอ (➕SKU+In Non-RB รวมเป็นปุ่มเดียว กดแล้วมีปุ่มใหญ่เลือก) 2) Scout/Backlog/สายพาน **ไม่ซ้ำซ้อนกันเชิงหน้าที่** (ตัดสินใจซื้อ/กรอกมอบหมาย/งานหลังขึ้นระบบ) แต่เป็น pipeline เดียวกัน → รวมบ้านเป็น hub เดียวเรียงตาม flow
- **Nav ใหม่:** หน้าแรก · สินค้า · เบิก · **➕ ของเข้า** (hub: SKU ใหม่/In Non-RB · จำทางล่าสุดใน localStorage `rb_inhub_last` · pill สลับบนหัวฟอร์ม · เฉพาะ L/A) · **🚀 ปล่อยสินค้า** (hub pills เรียง flow: Scout→Backlog→สายพาน→📅 ปฏิทิน→✍️ Caption · จำแท็บล่าสุด `rb_lnhub_last` · Scout pill ซ่อน non-L/A) · Expense · Setting — deep-link เดิมแก้ครบ (blRoute/lsBind → navIn) ฟังก์ชัน/สิทธิ์เดิมไม่แตะ
- **📅 ปฏิทินคอนเทนต์รวมทั้งร้าน (ชีต ContentPlan · action cpList/cpSave/cpDel ทุก role):** มุมมองรายสัปดาห์ จ-อา เลื่อน ◀▶ · ตัวนับเทียบเป้า 14 ชิ้น/สัปดาห์ · เพิ่ม/แก้/ลบ/ติ๊กเสร็จ (วันที่/ช่องทาง/Type Hook,Demo,Trust,Set,B-A,New,Promo/หัวข้อ/คนทำจากชีต Users) · **คลิปจากสายพานขั้น 7 โผล่อัตโนมัติ** (parse data.clips — ฟิลด์จริงคือ `{d,ty,hook,prod,proof,cta}` ไม่ใช่ date/type ⚠️ เกือบพลาด เช็คโค้ดรอบ 2 ก่อนแล้วแก้ทัน) แสดงเป็น 🚀 read-only · hint โครง Hook→Product→Proof→CTA + Tone เพื่อนสาว ติดหน้า
- **✍️ Caption Bank (ชีต CaptionBank · capList ทุก role / capSave+capDel = L/A+Graphic):** การ์ดแคปชั่น กรองตาม platform + **ปุ่ม 📋 คัดลอก** (clipboard + fallback execCommand) · **seed 8 แคปชั่นจาก Excel อัตโนมัติตอนชีตว่าง** (`capSeed_` — TikTok 3/IG 3/FB 2 ตาม Rainbow_Online_Content_Plan_v2.xlsx)
- **ทดสอบผ่าน local server ครบ:** nav 7 ปุ่ม ✓ chooser ครั้งแรก→จำทาง ✓ hub pills+ซ่อน Scout ตาม role ✓ ปฏิทินนับ 3/14+คลิปสายพานโผล่+ติ๊กเสร็จขีดฆ่า+เลื่อนสัปดาห์ ✓ payload cpSave/toggle/capSave ถูก ✓ Caption กรอง+ปุ่ม copy ✓ · syntax/dup/onclick ครบ ✓ · **ชีตใหม่ 2 ตัวสร้างเองตอนเปิดแท็บครั้งแรก ไม่แตะ DriveApp ไม่ต้อง initDrivePermission**
- **เก็บตก:** เพิ่ม permission allowlist ใน `.claude/settings.local.json` (Luffy รำคาญปุ่ม allow ก่อนนอน) — ไฟล์ gitignored อยู่แล้ว (global ignore) · Excel planner เลิกใช้ได้ ยกเว้นแท็บ Threads Plan + Checklist รายสัปดาห์ที่ยังไม่ย้าย (ใช้น้อย ไว้ค่อยว่ากัน)
- **คิวเช้า (จาก 23 ก.ค.):** แผนล้างทุนจม v2 รอ Luffy เคาะ · เมล Ketshop รอกดส่งใน Gmail Drafts · เก็บกวาด Code.gs (massBind217+Expenses ซ้ำ+เมนู 📧 ที่ไม่ใช้แล้ว)

## 💤 คิวค้างก่อนนอน 23 ก.ค. (Luffy สั่งจำไว้ เดี๋ยวค่อยว่ากัน)
- **วิเคราะห์ทุนจม Ketshop เสร็จแล้ว (v2 กรองตามเงื่อนไข Luffy: ตัดเสริมดวง-สต็อกร่วมหลายวันเกิด + ตัดรหัสแฟร์ไม่เชื่อม Shopee)** → เหลือของจริง 389 SKU / ~4,300 ชิ้น / ~1.08 ล้านบาท (A หยุดเบิก 290 ตัว 560k · B จัดโปร 54 ตัว 190k · C ดันคอนเทนต์ 45 ตัว 330k) · ไฟล์: Drive "ทุนจม Ketshop v2 กรองแล้ว (23 กค 69).csv"
- **แผนที่เสนอไว้ รอ Luffy เคาะ:** (1) A08 ห่วงชุบทอง 523 ชิ้น 284k เพิ่งแผ่ว → ทำคลิปดัน ไม่ลดราคา (2) SET2/4/5 1,553 ชิ้นไม่เคยขาย → แปลงเป็นของแถม BUY3GET1FREE (3) Minimal Organic 3 SKU 109k ชิ้นละ 1,100 → พระเอกคลิป (4) ห่วงห้อยจี้ CZ + แหวนพญานาค → ยุบ variant+ระบาย (5) เงินแท่ง 100g×3 (35.8k) นอนตั้งแต่ พ.ค. → ปล่อยก่อน · ทางเลือกลงมือ: ร่างตารางคลิป 2 สัปดาห์ตามฟอร์แมต planner หรือเปิดสายพาน 🚀 "ล้างทุนจม"
- **จดหมาย Ketshop:** draft อยู่ใน Gmail (จ่าหน้า sales@ketshopweb.com) รอ Luffy กดส่ง · สัญญา API/MCP รอเซ็น → ได้ client credentials มาเมื่อไหร่ กรอกผ่านเมนู 🛒 ใส่/แก้คีย์ ระบบติดทันที

## 🛒 23 ก.ค. 2569 (รอบ 3) — เชื่อม Ketshop OpenAPI เข้าเว็บ (B3.123/3.106 — อ่านอย่างเดียวล้วน deploy @125 ✓)
- **Base URL:** `https://openapi.ketshopweb.com` (เจอจากยิง /health ตอบ ok) · **auth:** POST /auth/token headers `x-oapi-client-id`/`x-oapi-secret` body `{scopes:["all"]}` → access_token มีอายุ (cache ใน ScriptCache 'ks_token' หมดอายุก่อนจริง 2 นาที · 401 = ล้าง cache ลองใหม่ 1 ครั้ง)
- **คีย์:** Luffy ใส่ `KS_API_KEY`/`KS_API_SECRET` (จากหน้า ส่วนเสริม Ketshop) ใน **Script Properties** เองแล้ว 23 ก.ค. — ไม่ผ่านแชท/ไม่เข้า git · UrlFetchApp มีสิทธิ์อยู่แล้ว (LINE ใช้) ไม่ต้อง authorize เพิ่ม
- **ของใหม่ (อ่านอย่างเดียวทุกตัว — ยังไม่มี write เข้า Ketshop สักจุด ตามกฎยืนยัน 3 ครั้ง):** helper `ksToken_`/`ksApi_` · action `ksLive` (dashboard+ยอดต่อช่องทางเดือนนี้ · L/A) / `ksStock` (สต็อกสดราย SKU สูงสุด 50 ตัว/ครั้ง · ทุก role) · **การ์ด 🛒 Ketshop สด หน้าแรก** (L/A · กดรีเฟรชเอง ไม่ auto) · **ปุ่ม 🛒 เช็คสต็อกสดใน modal สินค้า** (โชว์ Ketshop vs เรา + ผลต่าง · ไม่เจอ SKU = บอกตรงว่าชื่ออาจต่าง) · **เมนูชีต 2 ตัว:** 🛒 ทดสอบเชื่อม Ketshop (`menuKsTest`) + 🛒 เทียบสินค้า Ketshop↔คัมภีร์ (`menuKsCompare` — ดึงทั้งร้าน จับคู่ exact+prefix (sku KS ขึ้นต้น sku_rbo+'-') เขียนแท็บรายงาน '🛒 KsCompare': จับคู่ได้/มีแต่ KS/มีแต่เรา/ผลต่างสต็อก)
- **ทดสอบ:** logic ผ่าน stub บน local server ครบ (การ์ดสด/เช็ค SKU เจอ+ไม่เจอ) · syntax+dup+onclick ครบ · **⚠️ ของจริงรอ Luffy กดเมนู 🛒 ทดสอบเชื่อม Ketshop ในชีต 1 ครั้ง** (ผมเรียก action ที่ต้อง login ไม่ได้) แล้วค่อยกด 🛒 เทียบสินค้า → ได้รายงาน KsCompare เป็นฐานของปุ่ม push สต็อก
- **คิวต่อ (เขียน — confirm 3 ชั้น):** ปุ่ม ✅ Ketshop แล้ว push สต็อก increment ตามใบเบิก · ร่างจดหมาย Ketshop 4 ข้อ (บั๊ก search_order / ราคาตามช่องทาง / mapping ช่องทาง / API รูป)
- **⚖️ 23 ก.ค.: Luffy ได้รับสัญญา API/MCP จาก Ketshop — สั่งเป็นกฎเหล็กห้ามละเมิด** · สัญญาเป็นความลับ ห้ามลง repo (public) · checklist ปฏิบัติตามอยู่ Google Drive: `ketshop-compliance-checklist.md` + ความจำ Claude · สรุปหัวใจ: ระบบเราถูกทางอยู่แล้ว (คีย์ใน Script Properties ✓ ไม่มี token ในแชท ✓ confirm 3 ชั้น = Human-in-the-loop ✓) · **คิวงานตามสัญญา: ksApi_ ต้อง log ทุก call ลงชีต Log (เก็บ ≥90 วัน) + เตือนหมุนคีย์ทุก 90 วัน** · สัญญานี้น่าจะเป็นขั้นตอนก่อน Ketshop ออก client credentials ให้ (ตอบข้อ 1 ของจดหมาย)
- **⛔ อัปเดตดึก 23 ก.ค.: เชื่อม Code.gs → OpenAPI ยังไม่สำเร็จ — ติดที่ credentials** · ไล่จนสุดทางแล้ว: คีย์หน้า "ส่วนเสริม" (ขึ้นต้น d63c3a…) โดน `/auth/token` ตอบ **403 Unauthorized** (ลองคีย์ปลอมก็ 403 แบบเดียวกัน = คีย์คู่นี้ไม่ใช่ของ OpenAPI) · คู่มือทางการยืนยัน MCP connector ล็อกอินด้วย Domain+Username+Password (ไม่ใช้ API KEY) · ทุก endpoint auth ของ openapi.ketshopweb.com ต้องมี `x-oapi-client-id/secret` ชุดเฉพาะ ซึ่งต้องได้จาก "API console" ของ Ketshop (อ้างใน docs /oauth/v2/token) → **ทางเดียว: ถาม Ketshop** · เพิ่มเมนู 🛒 ใส่/แก้คีย์ Ketshop (`menuKsSetKeys` — กรอกผ่านหน้าชีต กันใส่ผิดโปรเจกต์) + error ละเอียดขึ้น (push HEAD แล้ว เมนูใช้ได้ ไม่ต้อง redeploy) · **ร่างจดหมายถึง Ketshop 5 ข้อเสร็จแล้ว: `ketshop_request_letter.md`** (ขอ client credentials / บั๊ก search_order / ราคาตามช่องทาง / mapping ช่องทาง / API รูป) — รอ Luffy ส่ง · ระหว่างรอ: งานอ่านผ่าน MCP ในแชท (Claude) ใช้ได้ปกติทุกอย่าง — การ์ด 🛒 หน้าเว็บ + เมนูเทียบในชีตจะติดทันทีที่ได้คีย์ถูกชุด

## 📐 23 ก.ค. 2569 (รอบ 2) — Luffy วางกฎ + สเปกเฟส C (คุยหลังเชื่อม MCP)
- **⛔ กฎเหล็กใหม่ (เข้า CLAUDE.md แล้ว): เขียนอะไรเข้า Ketshop ต้องถาม Luffy ยืนยัน 3 ครั้งเสมอ** — อ่านอิสระ · ฟีเจอร์เว็บที่เขียนเข้า Ketshop ต้อง confirm 3 ชั้นใน UI
- **ภาพการใช้ Ketshop จริง:** Shopee = master (ลงก่อน → copy ไป TikTok/Lazada → ส่วนเสริมอัปเดตจากแต่ละช่องเพื่อเชื่อม SKU + ตัดสต็อกร่วมคลังกลาง) · Shopee จำกัดตัวเลือก → SKU Shopee/Lazada บางตัวไม่ตรงกัน · หลายตัวลงแค่ Shopee (เชื่อมอยู่ 2,668 รายการ) · **ราคาใน Ketshop ≠ ราคาขายจริง marketplace** (ไม่ค่อยได้ใช้ — จะพัฒนาตอนทำเว็บ own-site www.rainbowsilveronline.com ที่ยังว่างเปล่า จดไว้เป็นงานอนาคต) · ค่าธรรมเนียม marketplace ปรับบ่อย → ปวดหัวไล่ปรับราคาทีละอัลบั้ม/SKU
- **สิ่งที่ Luffy อยากได้ (สเปกเฟส C ตัวจริง):** (A) ดึงข้อมูล Ketshop มาโชว์ในเว็บเรา — สต็อก/จำนวน/ยอดขาย ใกล้ real-time (B) **ปุ่ม ✅ Ketshop แล้ว ใน Ketshop Queue → เขียนสต็อกเพิ่มเข้า Ketshop จริงแบบ +เพิ่ม (increment)** เช่น Ketshop มี 5 เบิกมา 10 → ต้องเป็น 15 ไม่ใช่ 10 — ผ่าน flow เบิกเดิมทั้งหมด (C) บัญชีเดิมไม่แตะ (D) รูป: RBO SKU ใน Ketshop ที่ไม่มีรูป เติมจากคัมภีร์ได้ไหม + sync ยังไง
- **ข้อเท็จจริง API ที่เช็คแล้ว:** `PUT /product/update` รองรับ increase/decrease/adjust ราย SKU ต่อ warehouse/marketplace = ตรงสเปก (B) เป๊ะ · ราคากลางแก้ได้ (`PATCH /{sku}/basic` normal_price/discount_price) · **ราคาตามช่องทาง / mapping ช่องทางที่ลง / แก้รูป — API ไม่มี** · `search_order` ติดบั๊ก schema validation ฝั่ง Ketshop (เขาตีตกข้อมูลตัวเอง) → รวมเป็นจดหมายแจ้ง Ketshop 4 ข้อ
- **สถาปัตยกรรมที่วาง:** เว็บเรา (browser/public repo) ถือ token ไม่ได้ → **Code.gs เป็น proxy** เก็บ token ใน Script Properties (ห้ามเข้า git) เรียก Ketshop API → frontend โชว์สด · **Luffy ต้องขอ API token สำหรับ server-to-server จาก Ketshop** (token ของ MCP connector อยู่ฝั่ง claude.ai ดึงมาใช้ไม่ได้)

## 🔌 23 ก.ค. 2569 — เชื่อม Ketshop MCP connector สำเร็จ (ประตูเฟส C เปิดแล้ว)
- **Luffy ขอ MCP จาก Ketshop แล้วเชื่อมผ่าน connector (claude.ai) เรียบร้อย** — Claude เรียก API Ketshop ได้ตรง ๆ แล้ว: OpenAPI 126 endpoints (Product/Order/Customer/Warehouse/Report/Shipping/OmniChat) + เครื่องมือสำเร็จรูป ~20 ตัว · connector ผูกกับบัญชี Claude ใช้ได้ทุกเครื่องที่ login เดียวกัน ไม่มี token ในเครื่อง/ใน repo
- **ทดสอบอ่านจริงผ่าน 3 ชั้น (23 ก.ค.):** (1) `/report/dashboard-summary` → ร้านมี **1,117 SKU · หมดสต็อก 224 · ออเดอร์ค้าง 139** (2) `/product/search` ค้น "EA02" เจอ 32 ตัว SKU ตรงกับรหัส RBO เรา (เช่น `EA020340056-วงกลมถัก` instock 7 ราคา 145 — ฟิลด์ครบ: sku/price/instock/variants/category/img) (3) `/product/stocks/{sku}` ราย SKU ตอบ instock ต่อ warehouse (SKU ไทยต้อง URL-encode) — **นับสต็อกสด แก้สต็อก (`PUT /product/update`), ราคา (`/product/setprice`), stock_logs, low-stock-alert, dead-stock, sales-velocity มีให้ครบ = C1/C2/C3/C5 ทำได้จริงทั้งหมด**
- **นัยสำคัญ:** C3 (อ่านเมล Low Stock รายชั่วโมง) มีทางยกเครื่องเป็นเรียก `/report/low-stock-alert` ตรง · X1 (วิเคราะห์ยอดขาย Shopee จากไฟล์) บางส่วนแทนได้ด้วย `/report/*` สด ๆ · ช่องทางเข้า: MCP อยู่ฝั่ง Claude ไม่ใช่ฝั่ง Code.gs — สถาปัตยกรรม sync ต้องเคาะ (Claude เป็นตัวกลาง vs Code.gs เรียก Ketshop API เอง ต้องมี token ฝั่ง Apps Script)
- **คิวเดิมยังอยู่:** F5 รอบ 3 (D3 planner + Caption Bank) · เก็บกวาด Code.gs · ชีต 'ยังไม่มีrbws' 1,142 รายการ

## 🔩 22 ก.ค. 2569 (รอบ 6) — เพิ่มหมวด Spare part (B3.122/3.105 — deploy @124 ✓)
- **Luffy เคาะ prefix: SP (เงิน) / SPB (Brass) / SPS (Stainless)** — แบบเดียวกับหมวดอื่น · override ทีหลังได้ผ่าน Config แถว `prefix_Spare part`
- เพิ่มครบทุกจุด: dropdown หมวดฟอร์ม ➕SKU + 🛍In Non-RB + 📝Backlog (ใช้ CATS ร่วมกัน) · chips ประเภทหน้าคัมภีร์ + dropdown หน้า Edit + ค่า category ในชีต (CAT_TYPES) · ถอดประเภทจาก prefix รหัส SP*=Spare part ทั้งเว็บ+เมนูชีต 🏷 (TYPE_PREFIX ใส่ก่อน SE/SB/SS กันเทียบผิด — ทดสอบแล้ว Set ไม่โดนกิน) · SEED_CONFIG อัปเดตเผื่อติดตั้งใหม่
- ทดสอบผ่าน: dropdown มี Spare part ✓ SP001/SPB001/SPS001 → Spare part ✓ SE/SB/SS ยังเป็น Set ✓ category ในชีตพิมพ์ "spare part" ก็ validate ผ่าน ✓ syntax/dup ✓ · backup `rainbow online config 3.105.txt` ขึ้น Drive แล้ว

## 🌙 จบงาน 22 ก.ค. 2569 — สถานะก่อนกลับบ้าน (ทุกอย่าง sync ครบ ไม่มีของค้างเครื่อง)
- **ทุกชั้นตรงกันที่ 3.104 หมด:** เว็บจริง @123 = repo (B3.121 push แล้ว) = `apps-script/Code.gs` เครื่องที่ทำงาน = backup ใน Drive (`rainbow online config 3.104.txt`) — เครื่องที่บ้าน pull + โหลด backup จาก Drive ต่อได้เลย
- **งานวันนี้ (เครื่องที่ทำงาน):** F5 รอบ 2 ฟอร์มเฉพาะขั้น (B3.120/3.103) → deploy จากเครื่องที่ทำงานสำเร็จเป็นครั้งแรก (มี clasp+scriptId ครบแล้ว — `.clasp.json` อยู่ใน `apps-script/` gitignored) → แก้ 2 ปัญหาจากทีม: Draft ทับกันข้ามคน + ปุ่มดึงกลับ/ปฏิเสธใบขออนุมัติ (B3.121/3.104)
- **คิวถัดไป:** F5 รอบ 3 — D3 planner รวมทั้งร้าน + Caption Bank ย้ายจาก Excel เข้าเว็บ · เก็บกวาด Code.gs (ลบท่อน massBind217 ที่รันจบแล้ว + `SHEETS.Expenses` นิยามซ้ำ) · ชีต 'ยังไม่มีrbws' 1,142 รายการรอหา RBWS เพิ่ม · จับตา: ทีมใช้ปุ่ม ↩️ ดึงกลับ + draft หลายคนพร้อมกันจริง มีปัญหาแจ้งได้เลย

## 🐛 22 ก.ค. 2569 (รอบ 5) — แก้ 2 ปัญหาใช้งานจริงจากทีม (B3.121/3.104 — deploy @123 แล้ว ✓)
- **(1) Draft เบิกทับกันข้ามคน** — Jane คีย์ draft ไว้ → Ticha (หน้าเว็บเปิดค้าง state เก่า) มาคีย์ต่อ → ของ Jane หายทั้งชุด · **สาเหตุ:** `wdDraftSync` เขียนทับทั้งชีตด้วย state ในเครื่องของคนกดล่าสุด · **แก้:** action ใหม่ `wdDraftOp` — sync รายรายการ (set=upsert ตาม sku / del / delMany) ใต้ **LockService** กันเขียนชนกัน · ตอบ draft ล่าสุดทั้งชุดกลับทุกครั้ง → หน้าจอเห็นของเพื่อนโผล่ทันทีที่ตัวเอง sync · เปิดแท็บเบิกทีไรโหลด draft ใหม่เสมอ (เดิมโหลดแค่ตอน login) · ส่งใบเบิกแล้วลบเฉพาะ SKU ที่ส่ง (delMany — ของที่เพื่อนเพิ่งเพิ่มระหว่างนั้นไม่โดนล้าง) · `wdDraftSync` เดิมคงไว้เพื่อ client เก่าที่ยังไม่ refresh · โบนัส: `wdDraftList` ส่ง hist ด้วยแล้ว (เดิม breakdown ที่มาจำนวนหายตอน reload — บั๊กแฝง B3.50)
- **(2) กดขออนุมัติผิด ยกเลิกไม่ได้** — ใบ sent ไม่มีทางย้อน · **แก้:** action ใหม่ `wdRecall` + ปุ่มในแท็บ ⏳ ขออนุมัติ: Admin/Assist/Luffy เห็น "↩️ ดึงกลับแก้" · Store เห็น "✖ ปฏิเสธ" → ใส่เหตุผล (เว้นว่างได้) → ใบเปลี่ยนสถานะ `cancelled` (เก็บ audit ในชีต ไม่โผล่ในแท็บไหน) + note บันทึกใคร/เมื่อไหร่/เหตุผล → **รายการทั้งหมดเด้งกลับเข้า Draft** (SKU ซ้ำกับที่ค้างอยู่ = บวกจำนวนรวม · added_by = ผู้คีย์เดิม) → หน้าจอพากลับแท็บเบิกให้แก้/ลบแล้วส่งใหม่ · ดึงกลับได้เฉพาะสถานะ sent เท่านั้น (approved ไปแล้วเดิน flow ปกติ)
- **(3) แถม: "สวัสดี undefined"** — session เก่าบางตัวไม่มี name → โชว์ username แทน (`S.name||S.u`) · ถ้ายังเจอ ให้เช็คชีต Users ช่อง name ของคนนั้นว่ากรอกแล้ว
- **ทดสอบผ่าน local server ครบ:** Ticha เห็นของ Jane ตอนเปิดแท็บ ✓ เพิ่ม/แก้/ลบยิงเฉพาะรายการตัวเอง ของ Jane ไม่หาย ✓ ดึงใบกลับ→cancelled+ของกลับเข้า draft+เหตุผลส่งถึง backend ✓ ปุ่มแยก label ตาม role ✓ แท็บอื่นไม่มีปุ่ม ✓ ไม่มีฟังก์ชันซ้ำ/console error ✓
- **Deploy:** clasp push+redeploy @123 → เว็บจริงตอบ 3.104 ✓ ไม่แตะ DriveApp ไม่ต้อง initDrivePermission · backup `rainbow online config 3.104.txt` ขึ้นโฟลเดอร์ Drive แล้ว

## 🚀 22 ก.ค. 2569 (รอบ 4 — เครื่องที่ทำงาน) — F5 รอบ 2 เสร็จ: ฟอร์มเฉพาะขั้น (B3.120/3.103 — รอ deploy)
- **เคาะกับ Luffy ก่อนทำ:** ขั้น 5 sync `web_status` **ตอน L/A approve เท่านั้น** (ไม่ sync ทันทีที่พนักงานติ๊ก — มีคนกลั่นกรองก่อนเหมาทั้งอัลบั้ม) · เติมอย่างเดียวไม่ลบช่องทางเดิม
- **ของใหม่:** คอลัมน์ `data` ใน LaunchSteps (JSON ต่อขั้น · ensureHeaders_ เติมเอง · cap 20000) + ฟอร์มใน modal ขั้นตอน:
  - **ขั้น 3** ป้ายนับอัตโนมัติ: อัลบั้มนี้มี N SKU ในคัมภีร์ · L/A เห็นเพิ่ม กรอกครบ X / ⚠️ ไม่ครบ Y + รายชื่อตัวไม่ครบ (ใช้ BIBLE_FIELDS/skuMissing_ เดิม)
  - **ขั้น 4** checklist รูป 1-10 + 🎬 วีดีโอ พร้อมตัวนับสด N/11 (data.pics/vid)
  - **ขั้น 5** ติ๊กช่องทางจาก SALE_CHANNELS 11 ช่อง (data.channels) → **L/A กด ✅ approve → backend เติมเข้า web_status ทุก SKU ของอัลบั้ม** (จับคู่ Launches.name↔album_id trim+case-insensitive · เติม dedupe case-insensitive · ตอบ synced+channels · toast บอกจำนวน SKU ที่เติม · log launchSyncWeb)
  - **ขั้น 6** แผนการตลาด: Type คอนเทนต์ multi-select (Hook/Demo/Trust/Set/B-A/New/Promo — ภาษาเดียวกับ Excel planner) + โปรโมชั่น + กลุ่มเป้าหมาย/ช่องทางหลัก
  - **ขั้น 7** ตารางคอนเทนต์: รายการคลิป (วันที่ + Type + โครง Hook→Product→Proof→CTA) เพิ่ม/ลบ/แก้รายแถว แถวว่างกรองทิ้งตอน save (data.clips)
  - **ขั้น 9** วัดผล: Reach / Engagement / คลิกเข้า Shopee (ช่องเดียวกับ Performance Tracker)
  - ขั้น 1/2/8 ไม่มีฟอร์ม — save ไม่แนบ data (ไม่ทับของเดิม) · ฟอร์ม disabled เมื่อดูอย่างเดียว · data เดิม merge ไม่หาย (เริ่มจาก lnData_ ของเดิมเสมอ)
- **ทดสอบผ่าน local server ครบ:** ขั้น 3 ป้ายนับถูก (2 SKU · ไม่ครบ 1) ✓ ขั้น 4 ตัวนับ 3/11 + data ถูก ✓ ขั้น 5 ส่ง channels + เส้นทาง synced toast ✓ ขั้น 6/7/9 save→เปิดใหม่ค่าคงอยู่ (round-trip ผ่านคอลัมน์ data) ✓ คลิปแถวว่างถูกกรอง ✓ ขั้น 8 ไม่แนบ data ✓ · syntax Code.gs ผ่าน · ไม่มีฟังก์ชันซ้ำ · console ไม่มี error · (บั๊กที่เจอระหว่างเทส = stub เทสเอง ไม่ใช่โค้ดจริง)
- **✅ Deploy แล้ว (22 ก.ค. บ่าย — จากเครื่องที่ทำงาน):** เครื่องที่ทำงานมี clasp 3.3.0 + Node + login อยู่แล้ว (จากแชทแยก 21 ก.ค.) แต่ไม่มี scriptId → Luffy เปิดชีต→Apps Script→copy Script ID มาให้ → **แนวปฏิบัติ deploy ที่ใช้: `clasp pull` มาเทียบ SHA256 กับ backup 3.102 ใน Drive ก่อน (ตรงกัน byte-per-byte = ไม่มีใครแก้แทรก) → push → `clasp redeploy` ไป deployment เดิม @121 → curl ยืนยันเว็บจริงตอบ build 3.103** ✓ · ⚠️ ไฟล์หลักบนเว็บชื่อ `รหัส.js` (ไม่ใช่ Code.gs) — push ต้องใช้ชื่อนี้เสมอ ไม่งั้นกลายเป็น 2 ไฟล์ซ้ำ · `.clasp.json` (scriptId) เก็บถาวรที่ `apps-script/.clasp.json` แล้ว (gitignored) — **เครื่องที่ทำงาน deploy เองได้แล้วตั้งแต่นี้ไป** · สำเนา `rainbow online config 3.103.txt` อัปโหลดเข้าโฟลเดอร์ Drive "Project : Rainbow online ops" แล้ว (ผ่าน Drive API + token clasp — connector ส่งไฟล์ใหญ่ไม่ไหว)
- **คิวถัดไป (รอบ 3):** D3 planner รวมทั้งร้าน + Caption Bank ย้ายจาก Excel เข้าเว็บ · เก็บกวาด: ลบท่อน massBind217 + SHEETS.Expenses ซ้ำ

## 🚀 22 ก.ค. 2569 (รอบ 3) — F5 รอบ 1 เสร็จ + deploy แล้ว (B3.119/3.102 @120)
- **สเปกที่เคาะกับ Luffy วันนี้ก่อนสร้าง:** 1 สายพาน = 1 อัลบั้ม · สะพานเชื่อมอยู่หลัง Save SKU (ไม่ใช่ตอน route — ตรงนั้นราคายังไม่มี) · ส่งบัญชีอยู่ท่อเดิมช่วง ① ไม่แตะ · กริดเพิ่มอัลบั้มสไตล์ Shopee/Ketshop: **หัวข้อตัวเลือกทั้ง 2 ตั้งชื่อเองอิสระ** (แบบ/สี/ไซส์/ความยาว…) ตัวเลือก 2 มี/ไม่มีก็ได้
- **ของใหม่ 4 ชิ้น:** (1) **กริดเพิ่มอัลบั้ม 2 มิติ** (Backlog แท็บ ➕): ชื่ออัลบั้มครั้งเดียว + ตัวเลือก 1 (หัวข้อตั้งเอง + ค่าคั่น ,/บรรทัด · ช่วงเลข 3-10 แตกอัตโนมัติ) + ตัวเลือก 2 optional → จับคู่ทุก combination ชื่อ `ค่า1-ค่า2` · ตัวนับ SKU สด · ลบ combination ที่ไม่มีจริงรายตัว+คืนได้ · มอบหมายทั้งอัลบั้ม · บันทึกผ่าน backlogAdd เดิม (กริดแถวเดิมถูกแทนที่ — blAddRow/blRemoveRow ลบทิ้งแล้ว) (2) **สะพาน Save SKU→สายพาน** (`lnBridgeOffer`): หลัง addSku/extCreate สำเร็จ → ถ้าอัลบั้มยังไม่มีสายพาน active ถาม 1 ครั้ง (จำคำตอบทั้ง session ไม่ถามซ้ำ) → launchAdd พร้อม `doneSteps:[1,2]` ขั้น 1-2 done อัตโนมัติ note "อัตโนมัติ — เสร็จมาแล้ว…" · เฉพาะ L/A (เช็ค canManage จาก launchList) (3) **1 สายพาน = 1 อัลบั้ม**: launchAdd กันซ้ำ — ชื่อตรงกับสายพาน active = คืนตัวเดิม `{existed:true}` ไม่สร้างใหม่ · การ์ด board มีบรรทัด 🧬 ตัวเลือกในคัมภีร์ N SKU (ดึงจาก CAT สดตาม album_id trim+case-insensitive — **ระวัง: CAT เป็น let ห้ามเช็ค window.CAT จะ undefined เสมอ** เจอตอนเทสต์) (4) **ปุ่ม 🔁 ตีกลับ** (`lnStepReject`): L/A + สถานะ review → เหตุผลบังคับ → status doing + note นำหน้า "🔁 ตีกลับ: เหตุผล" (โน้ตเดิมพนักงานต่อท้ายไม่หาย ตีกลับซ้ำแทนบรรทัดเดิม) + แถวงานพนักงานโชว์เหตุผลสีแดงไม่ต้องเปิด modal
- **ทดสอบผ่าน local server (http-server :8123 — ไม่ใช่ file://) 5 ชุด:** กริด 2×8=16 ✓ ลบแล้ว save 15 ตัว payload ถูก ✓ สะพานถามครั้งเดียว/เกาะเงียบ/มีแล้วเงียบ ✓ ตีกลับเหตุผลว่างโดนกัน+note ถูก ✓ การ์ด 🧬 จับคู่อัลบั้ม ✓ · node --check + dup + onclick-มีนิยามครบ (สคริปต์เช็คต้องจับ `async function` ด้วย) ✓
- **Deploy แล้ว:** clasp @120 → curl ตอบ 3.102 ✓ · **ไม่แตะ DriveApp ไม่ต้องรัน initDrivePermission** · ไม่มีชีตใหม่/คอลัมน์ใหม่ (ใช้ Launches.name เป็นชื่ออัลบั้ม)
- **รอบ 2 คิวถัดไป:** ฟอร์มขั้น 6/7/9 ภาษาเดียวกับ Excel Luffy (Type Hook/Demo/Trust/Set/B-A/New/Promo · โครง Hook→Product→Proof→CTA · วัดผล Reach/Engagement/คลิก Shopee) + ป้ายนับอัตโนมัติขั้น 3 + checklist ขั้น 4 + ติ๊กช่องทางขั้น 5 (Luffy ยังไม่ตอบเรื่อง sync web_status — ถามก่อนทำ) → รอบ 3: D3 planner รวมทั้งร้าน + Caption Bank เลิกใช้ Excel

## 📑 22 ก.ค. 2569 (รอบ 2) — สารบัญแท็บใน Google Sheet (B3.118/3.101 deploy แล้ว @119)
- **โจทย์ Luffy:** หาแท็บใน Google Sheet ไม่เจอ (~30 แท็บ) + ต้องการย้ายงานจาก Excel กระจัดกระจายมาทำในเว็บแอปให้หมด
- **ทำแล้ว:** เมนูชีต **💰 RB ราคา → 📑 สร้าง/อัปเดตสารบัญแท็บ** (`menuBuildIndex`) — สร้างแท็บ "📑 สารบัญ" ไว้ตำแหน่งแรกสุด: จัดหมวด 6 กลุ่ม (สินค้า/คัมภีร์ · ของเข้า/เบิก · บัญชี · Ketshop/สต็อก · แพ็คเกจจิ้ง/ตีกลับ · ระบบ) + ลิงก์คลิกกระโดดไปแท็บ + คำอธิบายว่าเก็บอะไร + เปิดจากเว็บที่เมนูไหน + นับแถวข้อมูล · แท็บนอกผังโชว์ท้ายสุดอัตโนมัติไม่ตกหล่น · รันซ้ำได้ตลอด (เขียนทับ) · ไม่แตะ DriveApp ไม่ต้อง initDrivePermission — **⚠️ Luffy เปิดชีตแล้วกดเมนูนี้ 1 ครั้ง สารบัญถึงจะโผล่**
- **หมายเหตุโค้ด:** เจอ `SHEETS.Expenses` นิยามซ้ำ 2 ที่ (บรรทัด ~21 กับ ~25 — JS ใช้ตัวหลัง ตัวแรกเป็นซากเก่า ไม่พังแต่ชวนงง) รอเก็บกวาดรอบหน้าพร้อมลบท่อน massBind217
- **🗺️ Roadmap ย้าย Excel → เว็บแอป (เคาะกับ Luffy 22 ก.ค.):** ไฟล์ `Rainbow_Online_Content_Plan_v2.xlsx` (365 Day content planner — 7 ชีต: Overview/ปฏิทิน 4 สัปดาห์/Caption Bank/Checklist/Performance Tracker/Shopee Video Plan 14 คลิป/Threads Plan) = D3 ครึ่งหลังที่ Luffy เขียนไว้แล้วในรูป Excel → แผน: (1) F5 รอบแรก: สะพาน Backlog→สายพาน + 1 สายพาน=1 อัลบั้ม + ปุ่มตีกลับ (2) F5 รอบสอง: ฟอร์มขั้น 6/7/9 ใช้ภาษาเดียวกับ Excel (Type Hook/Demo/Trust/Set/B-A/New/Promo · โครงคลิป Hook→Product→Proof→CTA · ช่องวัดผล Reach/Engagement/คลิก Shopee) (3) D3 planner ในเว็บ: ปฏิทินคอนเทนต์รวมทั้งร้าน + Caption Bank ย้ายจาก Excel เข้าเว็บ — เลิกใช้ Excel

## 🚀 22 ก.ค. 2569 — F5 ปล่อยสินค้า deploy ขึ้นเว็บจริงแล้ว (B3.116→B3.117 / 3.99→3.100)
- **ที่มา:** Luffy ทำ F5 บนคอมที่ทำงาน (แชทแยก ไม่ผ่าน repo) แล้วหอบไฟล์กลับมาที่โฟลเดอร์ OneDrive `0.ทำเวป/22.7.2026/` (code_build2.gs + index.html + tracker ฉบับย่อ) → Claude ตรวจที่บ้าน: BUILD stamp ตรง 3 จุด (B3.117/3.100/3.100) ✓ · node --check ผ่าน ✓ · ไม่มีฟังก์ชันซ้ำ (86 ฟังก์ชัน) ✓ · ของเดิม 3.98 อยู่ครบ (massBind217/Backlog/ksConfirm/แชร์ PDF/Scout) ✓
- **Deploy แล้ว:** clasp push + deploy ไปที่ deployment เดิม (@118) → **เว็บจริงตอบ build 3.100 ยืนยันด้วย curl แล้ว** · index.html B3.117 วางเข้า repo + push แล้ว
- **F5 ไม่แตะ DriveApp → ไม่ต้องรัน initDrivePermission ซ้ำ** · ชีต `Launches` + `LaunchSteps` + Config แถว `launch_owners` สร้างเองอัตโนมัติตอนเปิดแท็บ 🚀 ครั้งแรก
- **F5 สรุป (สเปค Luffy 21 ก.ค.):** สายพาน 9 ขั้นต่อสินค้า 1 ตัว — 1.เจอสินค้า 2.ราคา/ทุน 3.รายละเอียดต่อช่องทาง 4.รูป10+วีดีโอ1 5.ลงขายทุกช่องทาง 6.แผนการตลาด 7.ตารางคอนเทนต์ 8.AI Gen รูป/คลิป 9.ติดตามผล · มอบหมายรายขั้น+กำหนดเสร็จ (เลยกำหนด=ขอบแดง ⏰) · แท็บ 👤 งานของฉัน (ทุก role เห็นเฉพาะขั้นตัวเอง) · สถานะ wait/doing/review/done/blocked + โน้ต + ลิงก์แนบ · ข้ามลำดับได้แต่ confirm เตือน · สิทธิ์เช็ค server-side: ดูได้ทุก role / อัปเดตขั้น=เจ้าของงานหรือ L/A / สร้าง-มอบหมาย-ปิดจบ=L/A · action: launchList/launchAdd/launchStepUpdate/launchAssign/launchSetStatus · **ชั้น approve (3.100):** พนักงานกดได้สูงสุด ✋ ส่งตรวจ (review) — ยิง done มา server แปลงเป็น review เอง · ✅ เสร็จ (approve) เฉพาะ L/A · คอลัมน์ sent_by/sent_at · แท็บงานของฉันฝั่ง L/A มีคิว ✋ รอ approve · default ผู้รับผิดชอบ 9 ขั้น: luffy,luffy,admina,graphic,admina,assist,assist,graphic,assist (แก้ใน Config)
- **หลัง deploy ต้องรู้:** พนักงานใหม่ (Ticha/Jane/Fran) เพิ่มใน ⚙️ Setting → Users ตามปกติ dropdown ดึงจากชีตจริง (active=Y) · username ที่มอบหมายต้องตรงชีต Users · line endings ไฟล์ผสม \r\n และ \n — patch ต้อง match ตามจริง
- **แผนแก้คอขวด "แบ่งงานไม่ลง" (X4 ใหม่ 21 ก.ค.):** แยกงาน 4 กอง (ปล่อยเลย/ปล่อยพร้อมกฎ/เก็บไว้แต่จด/เทอเท่านั้น) · F5 คือเครื่องมือหลัก — งานไปกองที่ขั้นที่มีเจ้าของ ไม่กองที่ Luffy · ก้าวต่อไป: เปิด `edit_fields_admin` เพิ่มให้ Admin เคลียร์คิว incomplete SKU เอง (ราคา/ทุนล็อกเหมือนเดิม) + SOP ให้ Thanya

## ✅ 22 ก.ค. 2569 — ชุดงาน 3.98 ปิดจบสมบูรณ์
- **Luffy รัน `initDrivePermission` เรียบร้อยแล้ว** — ครบทุกขั้นของรอบ 3.98 (deploy ✓ + สิทธิ์ Drive ✓) ฟีเจอร์ที่แตะ DriveApp ใช้งานได้จริงทั้งหมด: โฟลเดอร์ Backlog · PDF ใบเบิกไม่เด้งขอสิทธิ์ (แชร์โฟลเดอร์ ใบเบิก + ส่งบัญชีตามใบเบิกPDF) · Ketshop Queue ยืนยันหมู่
- **ค้างถัดไป:** D3 ครึ่งหลัง (Planner รายเดือน เขียว/เหลือง/แดง + งานเลยกำหนดยิงเข้า LINE digest — ครึ่งแรกเสร็จเป็น F5 แล้ว) · "วัดผล" (ช่วง ③) → F5 ขั้น 9 เริ่มรองรับแล้ว · ชีต 'ยังไม่มีrbws' (1,142 รายการ) รอหา RBWS เพิ่ม · ลบท่อน MB_ALBUMS/MB_ROWS/massBind217 ออกจาก Code.gs ได้ (รันจบแล้ว — ทำตอนแก้ Code.gs รอบหน้า ไม่ต้อง deploy แยก)

## 🌙 จบงาน 21 ก.ค. 2569 — สรุปวันนี้ (ทำต่อจากที่บ้านได้เลย)
- **✅ Code.gs 3.98 deploy สำเร็จแล้ว (21 ก.ค. ตอนดึก)** — เจอปัญหาไฟล์หายข้ามเครื่อง (OneDrive เข้าไม่ถึงจากเครื่องที่บ้าน) → **แก้ปัญหาระยะยาว: ย้ายมาเก็บไฟล์โปรเจกต์ผ่าน Google Drive แทน OneDrive** (เชื่อม Google Drive MCP connector เข้ากับ Claude แล้ว ใช้ได้ทุกเครื่องที่ login บัญชี Claude เดียวกัน ไม่ต้องเดินไฟล์เอง) — Luffy สร้างโฟลเดอร์ **"Project : Rainbow online ops"** ใน Google Drive ของตัวเอง (https://drive.google.com/drive/u/0/folders/1G_fmc3G91Gk5_IS7TAD3O-q9g1opD9gq) อัปโหลด Code.gs 3.98 (ชื่อไฟล์ `rainbow online config.txt`) + index.html + tracker + CLAUDE.md เข้าไป → Claude ดึงมาตรวจ (build 3.98 ✓, มี backlogList/backlogAdd/backlogUpdate/backlogRoute/massBind217/ksConfirm ครบ ✓, node --check ผ่าน ✓, ไม่มีฟังก์ชันซ้ำ ✓) → `clasp push` + `clasp deploy` ไปที่ deployment เดิม (@116) → **ยืนยันเว็บจริงตอบ build 3.98 แล้วด้วย curl** · **แนวทางใหม่: ต่อไปนี้ฝาก Code.gs สำรองไว้ที่โฟลเดอร์ Google Drive นี้แทน OneDrive**
- **~~ยังค้าง: รัน `initDrivePermission`~~ → ✅ รันแล้ว 22 ก.ค.** (3.98 แตะ DriveApp ใหม่: โฟลเดอร์ Backlog + setSharing บนโฟลเดอร์ใบเบิก/ส่งบัญชี)
- **งานที่จบวันนี้:** massBind217 ผูกรหัสหมู่ 739 ตัว (Luffy รันแล้ว — คัมภีร์ขึ้น 1,389 SKU) · B3.114 ถอดประเภทจาก prefix RBWS (EA/BR/BA/PE/NE/TO/AN + หมวดใหม่ Toe ring) · B3.115 Ketshop Queue ยืนยันหมู่ + แก้ PDF ใบเบิกเด้งขอสิทธิ์ (ฝั่ง gs)
- **ค้างงานถัดไป:** D3 (ถ่ายรูป+ลงช่องทาง+คอนเทนต์) และ "วัดผล" ยังเป็น concept · ชีต 'ยังไม่มีrbws' ในไฟล์ mass (1,142 รายการ) รอ Luffy หา RBWS เพิ่มแล้วค่อยผูกหมู่รอบใหม่
> **B3.115/3.98 (21 ก.ค. รอบ 2):** (1) **PDF ใบเบิกเลิกเด้งขอสิทธิ์ Google** — สาเหตุ: `savePdfToDrive_` ไม่เคย setSharing (ต่างจาก `htmlToPdfInFolder_` ที่แชร์ตั้งแต่ 3.78) → แก้: แชร์ระดับโฟลเดอร์ ใบเบิก + ส่งบัญชีตามใบเบิกPDF (PDF เก่าเปิดได้ด้วย) + รายไฟล์อีกชั้น + `initDrivePermission` เซ็ตแชร์ 2 โฟลเดอร์นี้ตอนรัน (2) **Ketshop Queue ยืนยันหมู่** — ปุ่มใหม่ ✅ Ketshop แล้ว (N) ข้างปุ่ม Export: ติ๊กรายตัว/ทั้งหมดแล้วกดทีเดียว (นับ/ส่งเฉพาะตัวที่ export แล้ว ตัวที่ยังไม่ export ข้ามพร้อมแจ้ง · backend `ksConfirm` รับ array อยู่แล้ว ไม่ต้องแก้) · ปุ่มรายตัวเดิมยังอยู่ · ทดสอบผ่าน mock ในเบราว์เซอร์ · **⚠️ ทั้งสองข้อรอวาง Code.gs 3.98 + deploy + รัน initDrivePermission** (ไฟล์เดียวรวมทุกอย่าง: Backlog + massBind217 + ประเภทจาก RBWS + อันนี้)
> **B3.114/3.98 (21 ก.ค.):** สินค้าที่ตกเป็น Non-Silver เพราะรหัส RBO ฟอร์แมตอิสระ (เช่นของที่ผูกหมู่ 739 ตัว) → **ถอดประเภทจาก prefix ของ SKU RBWS แทนอัตโนมัติ** (Luffy เคาะ: EA=Earrings · BR=Bracelet · BA=Bangle · PE=Pendant · NE=Necklace · TO=Toe ring · AN=Anklet) · เพิ่มหมวดใหม่ **Toe ring** (chips/หน้า Edit/ค่า category ในชีต รับหมด) · ลำดับ: category ในชีต > prefix RBO > prefix RBWS > Non-Silver · เมนูชีต 🏷 เติมประเภทสินค้า (menuFillCategory) ใช้ตรรกะเดียวกันแล้ว (3.98) · ทดสอบผ่าน 10 เคสในเบราว์เซอร์ · **frontend push แล้ว — backend รอวาง Code.gs + deploy (ตัวเดียวรวม Backlog + massBind217 + อันนี้)**
> **B3.113 (21 ก.ค.):** ถอดป้าย "🧪 ตัวอย่าง UI mock" ที่ค้างอยู่หน้า Backlog (ต่อชีตจริงแล้วตั้งแต่ B3.112) — เหลือ hint ขอบเขตสั้นๆ แทน · error "ไม่รู้จักคำสั่ง backlogList" บนเว็บจริง = backend ยังเป็น 3.96 **รอ Luffy วาง Code.gs 3.97 + deploy + รัน initDrivePermission** (ขั้นตอนอยู่ในสรุป 20 ก.ค. ด้านล่าง)

## 🔗 งานผูกรหัสหมู่ 21 ก.ค. 2569 — massBind217 (เขียนเสร็จ รอรัน)
- **โจทย์:** Luffy ค้นบันทึกเก่าเจอ mapping RBO SKU ↔ RBWS SKU **739 รายการ** (ไฟล์ `mass_21.7 ให้claudeทำไฟล์1.xlsx` แท็บ Sheet1: RBO/ชื่ออัลบั้ม/ชื่อตัวเลือก/RBWS · ตรวจแล้วสะอาด: RBO ไม่ซ้ำ ไม่มี RBO→หลาย WS · 295 WS ไม่ซ้ำ = หลายไซส์/สีใช้ WS เดียวกันตามกติกา · 41 อัลบั้ม) → เอาเข้าเว็บ: ผูก WS + ใส่ชื่ออัลบั้ม/ชื่อตัวเลือก
- **กติกาที่เคาะ:** RBO มีในคัมภีร์แล้ว = ยึดตัวเดิม+ผูก WS+**อัปเดตชื่ออัลบั้ม/ตัวเลือกตามไฟล์เสมอ**+เติมช่องว่างจาก PendingSku · ไม่มี = สร้างใหม่ (qty 0 เหมือน pendBind) · ตัวเดิมผูก WS อื่น = ไม่ทับ WS แต่ชื่อยังอัปเดต (รายงาน conflict) · ไม่ลบแถว PendingSku
- **ทำแล้ว:** ฟังก์ชัน run-ครั้งเดียว `massBind217Preview` (ดูสรุปก่อน ไม่เขียน) + `massBind217` (เขียนจริง) + ข้อมูล 739 แถวฝังใน Code.gs ท้ายไฟล์ (แบบเดียวกับ importSeed) · เขียนชีตแบบ bulk 2 ครั้งจบ กัน timeout · เทียบรหัส trim+case-insensitive · ทดสอบ logic ผ่าน mock ครบ 4 เคสในเบราว์เซอร์แล้ว · **รันเสร็จแล้วลบท่อน MB_ALBUMS/MB_ROWS/ฟังก์ชัน ออกจาก Code.gs ได้เลย**
- **⚠️ ขั้นตอน Luffy:** (1) วาง Code.gs ใหม่ (รวม Backlog 3.97 + massBind217 ในไฟล์เดียว) → Deploy → Edit → New version (2) รัน initDrivePermission (3) **Run ▶ massBind217Preview ดูตัวเลขก่อน** (4) ถูกใจแล้ว Run ▶ massBind217 · ชีต 'ยังไม่มีrbws' ในไฟล์ Excel (1,142 รายการยังหา WS ไม่เจอ) ไม่เกี่ยวกับรอบนี้ — ไว้หาเพิ่มแล้วค่อยทำรอบใหม่
> **กติกา: จบงานทุกครั้ง Claude ต้องอัปเดตไฟล์นี้ก่อน push git**

## 🌙 จบงาน 20 ก.ค. 2569 — สรุปวันนี้
- **F4 Backlog — ปุ่ม "ส่งไป SKU ใหม่/In Non-RB"** ✅ เลิก mock แล้ว (B3.111) — กดแล้วพาไปหน้า ➕ SKU หรือ 🛍 In Non-RB จริง เปิดโหมด New SKU ให้เอง เติมข้อมูลจาก Backlog ให้ครบ (หมวด/Class/รหัส/อัลบั้ม/ตัวเลือก/น้ำหนัก/Location · ฝั่ง SKU ใหม่เติม RBWS+Location RBWS+สี RBWS ด้วย) **+ แนบรูปที่ถ่ายไว้ตอน Backlog ให้อัตโนมัติ**
- **F4 Backlog — backend จริง** ✅ เขียนแล้ว (B3.112/3.97) — Luffy ได้ส่ง Code.gs ตัวจริงมาให้ (เก็บสำเนาไว้ที่ `apps-script/Code.gs` ในเครื่อง — โฟลเดอร์นี้ `.gitignore` กันไว้ ไม่ขึ้น repo public):
  - **Code.gs (3.97):** ชีตใหม่ `Backlog` (bl_id/status/album/variant/assigned_to/category/cls/sku_rbo/rbws_code/weight/loc_rbo/loc_rbws/rbws_color/img/created_by/created_at/updated_at/routed_at) · action ใหม่ 4 ตัวตามแพทเทิร์น Scout: `backlogList` (ทุก role) / `backlogAdd` (batch จากกริด+Scout import · เลขรัน BL ผ่าน nextDoc_) / `backlogUpdate` (กรอกรายละเอียด · รูปอัพ Drive โฟลเดอร์ `Rainbow Online/Backlog/` · เช็คครบ 11 ช่องฝั่ง server → ready/draft เอง · กันแก้ตัวที่ส่งต่อแล้ว · assigned_to แก้ได้เฉพาะ L/A) / `backlogRoute` (L/A เท่านั้น → sent_sku/sent_nonrb) · แถม: `extCreate` รองรับ `it.img_url` (รูปจาก Backlog ส่ง URL ตรง ไม่อัพซ้ำ — แบบเดียวกับ importExistSku)
  - **index.html (B3.112):** Backlog โหลด/บันทึกผ่านชีตจริงทุกแท็บ — **ข้อมูลไม่หายเมื่อรีเฟรชแล้ว** · Scout "นำเข้า Backlog" ลงชีตจริง · route แล้วรูปส่งต่อเป็น URL (addSku ผ่าน `q.img` ที่ backend มีอยู่แล้ว / In Non-RB ผ่าน `img_url` ใหม่)
  - **ทดสอบผ่านเบราว์เซอร์แล้ว** (stub API จำลอง backend เพราะเครื่องนี้ deploy ไม่ได้): เพิ่มจากกริด→ได้ bl_id จริง / กรอกครบ→ready อัตโนมัติ / ล้าง cache แล้ว blLoad คืนข้อมูลครบ (จำลองรีเฟรช) / route ทั้ง 2 เส้นทาง prefill ถูก+รูป URL ติดไป / ebnAdd พก img_url เข้าบิล / Scout import ลงชีต · `new Function(Code.gs)` syntax ผ่าน · ไม่มีฟังก์ชันซ้ำ · console ไม่มี error
  - **⚠️ รอ Luffy ทำ 2 ขั้นก่อนใช้จริง:** (1) วาง Code.gs ใหม่ทับใน Apps Script → Deploy → Manage deployments → ✏️ Edit → New version → Deploy (2) รัน `initDrivePermission` ซ้ำ 1 ครั้งกันเงียบ (มีโฟลเดอร์ Drive ใหม่ `Backlog`) — ชีต Backlog สร้างเองอัตโนมัติตอนเปิดแท็บครั้งแรก · **ห้าม refresh เว็บก่อน deploy backend** ไม่งั้น sysCheck จะเตือนเวอร์ชันไม่ตรง (frontend 3.97 / backend 3.96)
- **ค้างต่อ:** D3 (ถ่ายรูป+ลงช่องทาง+คอนเทนต์) และ "วัดผล" ยังเป็นแค่ concept ในตาราง Flow ด้านล่าง ยังไม่เริ่มสร้าง

---

## ✅ เสร็จแล้ว (ยืนยันจากโค้ดจริง)
| โมดูล | หมายเหตุ |
|---|---|
| 🔵 ของเข้า 2 ทาง | แท็บ เบิก (4 สถานะ) + In Non-RB |
| 🟣 คัมภีร์สินค้า | ครบ · **B3.99: สิทธิ์แก้ต่อ role ย้ายไปชีต Config แถว `edit_fields_admin` / `edit_fields_graphic` / `edit_fields_storefront` (คั่น comma · default img,loc_rbws,loc_rbo = เท่าของเดิม) — Luffy เปิด/ปิดเองได้เพื่อกระจายงานให้พนักงานเติมข้อมูล · ⛔ ล็อกตาย L/A เสมอไม่ว่า Config เขียนอะไร: cost_latest, cost_note, color_rbo, price ทั้ง 5 (SKU_LOCKED_) · บังคับ 2 ชั้น หน้าจอ+backend (skuUpdate กรอง ALLOW+imgOK) · ไม่มีสิทธิ์สักช่อง = ไม่โชว์ปุ่ม Edit · ติ๊กช่องทางขายเปิดให้พนักงานได้ผ่าน web_status** · **B3.100: ชีต Selections เพิ่ม 3 คอลัมน์ (ensureHeaders_ สร้างให้เองตอนเปิดเว็บ): `category` (ประเภท — ว่าง=ถอดจาก prefix อัตโนมัติ กรอกทับได้ทั้งในชีตและหน้า Edit dropdown) · `price_rec` (Recommended price ตัวเลข ล็อก L/A) · `tags` (คำค้นอิสระ คั่น , — ช่องค้นหาเจอด้วย) · ช่องทางขายแก้ในชีตได้ที่คอลัมน์ `web_status` เดิม (ชื่อช่องทางคั่น , เช่น Ketshop,Shopee) · prefix `FB`=Bracelet (แก้บั๊ก FB-BRx ขึ้น Non-Silver) · การ์ดสินค้ามีป้ายประเภทข้าง SKU · ปุ่ม ⚠️ รายละเอียดไม่ครบ ย้ายขึ้นบนข้างหัวข้อ** · **B3.101: แถวจุดสีราคา Mix(สายรุ้ง)→Brown ใต้ chips ช่องทาง (หน้าตาเดียวกับหน้าแรก · toggle · วงแหวนชมพูตอน active) · กรอง 4 ชั้นพร้อมกัน: ค้นหา+ประเภท+ช่องทาง+สี (เช่น แมว+Earrings+Lazada+Brown) · ถอดกฎ B3.60 พิมพ์แล้วล้างสีออกแล้ว — จุดสีหน้าแรก (catColorGo) ยังล้างค้นหา+สลับแท็บเหมือนเดิม · tags เว้นวรรครอบ comma ได้อิสระ (ค้นแบบ substring)** · **B3.102/3.90: เมนูชีต 💰 RB ราคา → 🏷 เติมประเภทสินค้า (menuFillCategory) เติม category เฉพาะช่องว่างทั้งชีตรวดเดียว (ค่าที่พิมพ์เองไว้ไม่แตะ · ตรรกะ skuTypeGs_ ตรงกับหน้าเว็บรวม FB=Bracelet · ไม่เข้าหมวด=Non-Silver)** · **B3.97 (17 ก.ค.): ค้นหา 4 ช่อง (SKU RBO/RBWS/อัลบั้ม/ชื่อตัวเลือก · partial+case-insensitive) · chips กรองประเภทสินค้า 8 หมวด+Non-Silver (ถอดจาก prefix SKU อัตโนมัติ longest-match — BAB/BAS ก่อน BA · **B3.98: Non-Silver = ของที่ไม่ใช่เครื่องประดับเลย (prefix ไม่เข้า 8 หมวด) — EB/ES/RB/BAB ยังอยู่หมวดเครื่องประดับตามเดิม → ทุก SKU มีประเภทเสมอ**) · chips ช่องทางขาย 11 ช่อง+ตัวเลขนับ (ทุก role) · chip ⚠️ SKU รายละเอียดไม่ครบ (L/A) โชว์ว่าขาดช่องไหนบนการ์ด · ฟิลด์เช็คอยู่ชีต Config แถว `bible_check_fields` (สร้างแถวให้อัตโนมัติผ่าน cfgEnsure_) default: pack_class, color_rbo\|price_base, sku_rbo, album_id, sel_name, cost_latest, weight, img · กติกา: comma=ต้องมีทุกตัว · \|=มีอย่างใดอย่างหนึ่งก็ครบ · ค่าว่างหรือ 0 = ยังไม่กรอก** |
| 🟡 ราคา 5 ตัวเลข | ชุด A/B/C + ปุ่มปรับราคาทั้งระบบ + เมนู 💰 ในชีต |
| 🟠 บัญชี 2 บริษัท | Etax csv + พี่ตุ่น csv + Expense ครบ (Edit บิลได้) |
| Ketshop Queue | รับของ→คิว→PDF→ยืนยัน→ประวัติ |
| เช็คสต็อก (นับ) | โหมดร้าน/แฟร์ · แก้ inline · Export .xlsx · **B3.79: การ์ดแนวตั้งจบจอเดียว (เลิกตารางกว้าง) + 🖨️ ใบเช็คสต็อก PDF เรียงตาม Location ช่อง "นับได้" เว้นว่างให้พนักงานเขียน (blind count) · B3.81: ฝังรูปเสมอ (Drive thumbnail sz=w96 ~3KB/รูป · fetchAll ก้อนละ 100 · stockPdf เข้า HEAVY 4 นาที) + B3.82: คอลัมน์ ชื่ออัลบั้ม/ชื่อตัวเลือก มีหัวตาราง · Location ป้ายข้าง SKU · thead ซ้ำทุกหน้า (แบ่งกันนับได้) · เลือกบาง SKU มี autocomplete รูปจาก CAT · B3.85: โฟลเดอร์ เช็คสต็อก แชร์ anyone-with-link — ไม่ต้องขอสิทธิ์รายใบ · เซฟ Drive: Rainbow Online/เช็คสต็อก/** |
| นำเข้า SKU เดิม | RBWS/ภายนอก ไม่แตะบัญชี · **16 ก.ค.: นำเข้า RBWS 497 SKU จาก 82 ไฟล์ catalogue (.xls มี merge cell+รูปฝัง) → PendingSku รอผูกรหัส RBO · B3.90 แก้: importData เติมรูปให้ pending เดิมที่ยังไม่มีรูปตอนอัพซ้ำ + image_base64 ต้องมีหัว data:image/jpeg;base64, · B3.91 แก้ timeout เติมรูป (อ่านชีตครั้งเดียว เขียนตรงเซลล์) ไฟล์นำเข้าซอย 50 SKU/ไฟล์ · B3.92: ฟอร์มนำเข้า SKU (RBWS) พิมพ์รหัส RBWS ≥3 ตัว → autocomplete จาก PendingSku เติมข้อมูล+รูปเดิมให้ (img_url ไม่อัพรูปซ้ำ) · กติกา: RBWS ซ้ำได้หลาย RBO · RBO ห้ามซ้ำ (backend เช็คอยู่แล้ว) · หลายไซส์/สี = RBO คนละตัว RBWS เดียวกัน · B3.93 แก้ชิปช่องทางในฟอร์มกดไม่ได้ (บั๊กเก่า B3.57: clickable=false ทำ onclick ไม่ถูกสร้าง) + save error เด้ง toast · B3.94: ฟังก์ชัน g2 หายทั้งไฟล์ (ฟอร์มเรียก 20+ จุด → Save/hint ตายเงียบ) ใส่คืนแล้ว — บทเรียน: health check ควรเช็คฟังก์ชันที่ถูกเรียกแต่ไม่มีตัวตนด้วย · B3.95: การ์ดรอรหัสโชว์เฉพาะตัวยังไม่ผูก (pendingList แนบ used) ฟอร์มยังค้นตัวผูกแล้วได้พร้อมป้าย ✓ · **B3.104/3.92: ⚡ ผูกรหัสด่วนที่การ์ดรอรหัสหน้าแรก — ช่องค้นหา (RBWS/รายละเอียด · โชว์ 15 ปกติ/50 ตอนค้น) + dropdown เลือก prefix ครั้งเดียว (24 prefix มีชื่อไทย) → กด ⚡ ทีละการ์ด เลขรันอัตโนมัติผ่าน nextSkuByPrefix_ (แยกจาก nextSku_ · เคารพ sku_start_XX ใน Config) หรือพิมพ์รหัสเองในช่องก่อนกด · backend action pendBind: copy PendingSku→Selections (detail/weight/color_rbws/loc_rbws/img/cost→cost_latest · qty_shop=0) เช็ครหัสซ้ำ · ผูกแล้วการ์ดหาย+คัมภีร์รีเฟรชทันที · เหตุผล: Ketshop ไม่มีข้อมูล RBWS การผูกต้องเกิดที่ระบบเรา แล้วเอารหัส RBO ไปลง Ketshop · รายละเอียดที่เหลือเติมทีหลังผ่านปุ่ม ⚠️ ไม่ครบ** · **B3.105/3.93: ของเก่าส่วนใหญ่มีรหัส RBO อยู่แล้วฟอร์แมตอิสระ (RB009RT-ขาว / A016แบบที่5 / หยดน้ำ5X8#สีเหลือง) → ช่องรหัสบนการ์ดเป็น autocomplete ค้นคัมภีร์ (sku_rbo/album_id/sel_name/detail · ≥2 ตัวอักษร · โชว์ 6 ตัว · ป้ายแดงถ้าตัวนั้นมี WS แล้ว) · pendBind ฉลาด: รหัสมีอยู่ในคัมภีร์=ผูกเข้าตัวเดิม (เติม sku_rbws + เติมเฉพาะช่องว่าง detail/weight/color_rbws/loc_rbws/cost_latest/img — ไม่ทับของเดิม · ถ้าตัวเดิมผูก WS อื่นอยู่ = error บอก) · ไม่มี=สร้างใหม่ · เลิกบังคับตัวใหญ่ เทียบ case-insensitive รหัสไทยได้ · toast แยกโหมด 🔗 ผูกตัวเดิม / ✓ สร้างใหม่** |
| ~~R4 แก้ qty หลังรับของ~~ | **ตัดสินใจ+ทำแล้ว** ตั้งแต่ B3.5x: qty ล็อกถาวรหลัง received แก้ได้เฉพาะ นน.ชั่งจริง (`wdEditReceived`) |
| 🔒 Security | **B3.94/3.94 (18 ก.ค.): ปิดช่องโหว่ `?dbg=exp` ใน `doGet`** — เดิมใครก็ยิง URL เว็บแอป + `?dbg=exp` ได้โดยไม่ต้อง login แล้วโค้ดจะเขียนจริง (`PING` test) + อ่านข้อมูลจริงจากชีต Expenses (headers, จำนวนแถว, ยอด/เดือน, แถวแรก) ออกมา ตอนนี้ `doGet` ตอบแค่ `{ok,system,build,time}` เสมอ ไม่แตะชีตใดๆ · เจอระหว่างสแกน repo หา token/secret หลุด 18 ก.ค. · ยืนยันแล้วบนเว็บจริงหลัง deploy: `?dbg=exp` คืนแค่ status ปกติ build 3.94 |

---

## 🔒 Security — ค้าง (เจอตอนสแกนหา token/secret หลุด 18 ก.ค. 2569 — ยังไม่จัดลำดับเข้าเฟส)
| # | งาน | หมายเหตุ | สถานะ |
|---|---|---|---|
| S1 | Session token แยกจาก password hash | ตอนนี้ `key` ที่ frontend เก็บไว้ = `x.passhash` ตรงๆ (`verify_` เทียบ `passhash===k` ตรงๆ) ไม่มี session แยก ไม่มีวันหมดอายุ ไม่มี revoke รายอุปกรณ์ — ถ้า `key` หลุด (localStorage/log/MITM) = เข้าถาวรจนกว่าเจ้าของจะเปลี่ยนรหัสผ่าน | ⬜ |
| S2 | Hash แข็งขึ้น + rate limit ที่ login | `hash_()` ใช้ SHA-256(password+salt ตายตัว `'RBOsalt'`) เป็น fast hash ไม่มี per-user random salt · รหัสผ่านขั้นต่ำแค่ 4 ตัว → ถ้าชีต Users หลุด brute-force ได้เร็ว · ไม่มี rate limit/lockout ที่ `action:'login'` เลย ถูกสุ่มรหัสผ่านไม่จำกัดครั้งได้ | ⬜ |

### 📝 บันทึกเพิ่ม (18 ก.ค. 2569): TOKEN_SECRET เก่าใน git history
- **repo `re-order`** (คนละ repo กับ rainbow-online-ops นี้) มี git history เก่า commit `d23c331` ที่เคยมี `TOKEN_SECRET` ฝังอยู่
- **rotate แล้ว** — token ตัวนั้นใช้ไม่ได้แล้ว (inert) ต่อให้มีคนขุด history เจอก็เอาไปใช้อะไรไม่ได้
- **ตัดสินใจ:** ปล่อย history ของ repo `re-order` ไว้แบบเดิม ไม่ล้าง/ไม่ rewrite history เพราะความเสี่ยงจริง = 0 (token inert แล้ว) — การ rewrite history มี cost/risk สูงกว่าประโยชน์ที่ได้
- repo นี้ (`rainbow-online-ops`) ยังคงเป็น **public** ต่อไป เพราะเป็นเว็บใช้งานจริงผ่าน GitHub Pages (Pages แบบ custom domain/private ต้องใช้ GitHub Pro) — ถ้าจะเปลี่ยนเป็น private ต้องอัปเกรดแผนก่อน

## 🔥 เฟส A — เก็บกวาด + ของค้างชิ้นเล็ก (ทำรวดเดียว 1 build)
| # | งาน | ขนาด | สถานะ |
|---|---|---|---|
| A1 | ล้างไฟล์ Excel เล่มผีใน Drive (ul-NaN/Wed/Tue) — เขียนสคริปต์สแกน+ลิสต์ให้ดูก่อนลบ | S | ⬜ |
| A2 | PDF แยกตอน Assist ยืนยันรับของ (`wdReceive` ยังไม่ gen PDF) | S | ⬜ |
| A3 | แท็บประวัติบัญชีย้อนหลังใน UI | M | ⬜ |
| A4 | หน้าดู log (backend `log_()` มีแล้ว ขาดแค่จอ) | S | ⬜ |

## 🔴 เฟส B — ออกงานแฟร์ (โมดูลใหญ่สุด · 7 ขั้น)
| # | ขั้น | สถานะ |
|---|---|---|
| B1 | สร้างรอบแฟร์ + เคาะราคาสีประจำรอบ | ⬜ |
| B2 | ตั้งโควต้า สี×หมวด×จำนวนแบบ (ดึงเฉพาะมีสต๊อก) | ⬜ |
| B3 | Picking List PDF มีรูป + Admin ติ๊กเก็บของ | ⬜ |
| B4 | ยกออก: สต๊อกร้าน → สต๊อกแฟร์ (เช่น 100→แฟร์5/ร้าน95) | ⬜ |
| B5 | ขายหน้างานผ่าน POS (Ketshop — นอกระบบเรา บันทึกยอดเข้า) | ⬜ |
| B6 | นับจริง เทียบ ควรเหลือ=ยกไป−POS เขียว/แดงรายตัว | ⬜ |
| B7 | เคลียร์ส่วนต่าง+หมายเหตุ รับกลับ ปิดรอบ สรุปต่อสี/หมวด | ⬜ |

## ⚪ เฟส C — สต๊อกอัจฉริยะ (ครึ่งหลังของโมดูลสต๊อก)
| # | งาน | สถานะ |
|---|---|---|
| C1 | นำเข้า snapshot Ketshop เป็นฐาน ("Ketshop เป็นนาย" — เคาะแนวทางแล้ว) | ⬜ |
| C2 | Sync สต๊อกสองทางกับ Ketshop | ⬜ |
| C3 | อ่านเมล KETSHOP Low Stock อัตโนมัติ | ✅ B3.75: สแกนรายชั่วโมง → LowStockAlerts → การ์ดหน้าแรก · เข้าเงื่อนไขเฉพาะ <5 (Config `ls_threshold`) · เติมของ ≥5 ปิดอัตโนมัติ · คลังร่วม 1 รายการ/SKU · กันซ้ำ `ls_last_scan` · **parser: เมล Ketshop = HTML ปนใน plain + `<td>` คั่นด้วย newline → ต้องยุบ newline ก่อนแปลง `</tr>`=บรรทัด `</td>`=tab (ทดสอบผ่านกับเมลจริง 15 ก.ค.)** · แก้ parser เมื่อไหร่รัน `lsRescan` · **B3.78: การ์ดมีปุ่ม 📝 เบิก (ลง Draft + ปิดรายการ note "สั่งเบิกแล้ว") / ➕ ผูกรหัส (เปิดแท็บ add เติมรหัสให้ · L/A เท่านั้น)** |
| C4 | คิวเช็คเติมของเทียบเป้าตามดาว · ใกล้ 0 เด้งทันที | ⬜ |
| C5 | ประวัตินับ → อัตราไหล → แนะนำเบิก/เลื่อนดาว | ⬜ |
| C6 | รอบนับใหญ่รายไตรมาส | ⬜ |

## 🟢🟤↩️ เฟส D — โมดูลปฏิบัติการที่เหลือ
| # | โมดูล | สถานะ |
|---|---|---|
| D1 | 🟤 สิ้นเปลือง/แพ็คเกจจิ้ง: รับเข้า→บันทึกเช็ค→อัตราใช้/วัน→เตือนซื้อ | ⬜ |
| D2 | ↩️ ตีกลับ/คืน/เปลี่ยน: แจ้งเคส→เช็คสภาพ→ดี=เข้าสต๊อก / ตำหนิ=แยก | ⬜ |
| D3 | 🟢 Launch checklist + Planner รายเดือน เขียว/เหลือง/แดง · **16 ก.ค.: Luffy บอกงานค้างที่ตัวเองเยอะ — ยกความสำคัญขึ้น: pipeline ลงสินค้าใหม่ (ถ่ายรูป→ราคา→ลง Ketshop→ลงช่องทาง→เปิดขาย มอบหมายรายขั้น) + planner คอนเทนต์ 14 คลิป/สัปดาห์** | ✅ **ครึ่งแรกเสร็จเป็น F5 🚀 ปล่อยสินค้า (B3.116-117/3.99-3.100 — ดูสรุป 22 ก.ค. บนสุด)** · เหลือ: Planner รายเดือน เขียว/เหลือง/แดง + งานเลยกำหนดยิงเข้า LINE digest |

## 🆕 ฟีเจอร์ใหม่ที่คุยไว้ 18-19 ก.ค. 2569 — ยังไม่เข้าคิว
> บันทึกไว้กันหาย เพราะคุยยาวใน chat แล้ว context จะหายตอนแชทใหม่ — รายละเอียดสเปกเต็มอยู่ในนี้แล้ว พร้อมเริ่มออกแบบ/สร้างได้เลยเมื่อสั่ง

### 🗺️ Flow งานเต็ม — Timeline 11 ขั้น (Luffy วางไว้ 19 ก.ค. รอบ 3 — เคาะเขตแดนแล้ว กันงง/กันปนกัน)
> Luffy ให้ timeline เต็ม 11 ขั้น แล้วถามเองว่า "จะปนกันไหม" — เคาะแล้วว่า**ไม่ปน** ถ้าตัดเป็น 3 ช่วงชัดๆ ส่งต่อกันเป็นทอดๆ (ห้ามยัดรวมเป็นฟีเจอร์เดียว):

| ช่วง | Timeline ข้อ | โมดูลรับผิดชอบ |
|---|---|---|
| **① เข้าระบบ** | 1.สินค้ามา · 2.ทำต้นทุนได้ราคาได้ SKU · 3.ได้รายละเอียดพร้อมลงช่องทาง · 4.ส่งบัญชี | **F4 Backlog Board** — **จบที่ข้อ 4 (ส่งบัญชี) เท่านั้น** พอมี SKU+ต้นทุนแล้วออกจาก Backlog ไปเลย ไม่ต้องแบกต่อ |
| **② คอนเทนต์** | 5.ถ่ายรูป 10 รูป+ปก · 6.รายละเอียดสินค้าแบบยาว · 7.ลงสินค้าตามช่องทางที่วางไว้ · 8.Video แผนลงคอนเทนต์ | **D3** เดิม (เฟส D ด้านล่าง — Luffy ยกความสำคัญไว้แล้ว 16 ก.ค.) |
| **③ วัดผล** | 9.ติดตามผล · 10.ถ้าขายไม่ได้ทำยังไง · 11.Up sell | **ช่องว่างใหม่ ยังไม่สร้าง** — ยังไม่เคาะรายละเอียดว่าวัด/ทำยังไง รอ Luffy คิดต่อ |

**เหตุผลที่แยก:** ยัดทุกอย่างเข้า Backlog Board ตัวเดียวจะกลายเป็นฟีเจอร์ยักษ์ทำไม่เสร็จสักที แยกเป็น 3 ช่วงเล็กสร้างเสร็จไวกว่าและใช้จริงได้เร็วกว่า

| # | ฟีเจอร์ | สเปก/หมายเหตุ | สถานะ |
|---|---|---|---|
| F1 | 🔎 Scout หา New SKU จากซัพพลายเออร์ | Luffy หรือ Assist manager (role L/A) เป็นคนเจอสินค้าแล้วแอดเข้าระบบเอง (ไม่ใช่ auto-scrape) · ฟิลด์: แหล่งที่มา, วันที่/เวลา, รูปภาพ, น้ำหนัก, ต้นทุนต่อชิ้น, ต้นทุนต่อกรัม, สกุลเงิน (+อัตราแลกเปลี่ยนถ้าไม่ใช่ THB), ในไทย/ต่างประเทศ, **หมายเหตุ** (เพิ่ม 19 ก.ค.) · ใส่ต้นทุนแล้ว**ระบบแนะนำสีราคาให้อัตโนมัติ** ใช้ของเดิมทุกอย่าง ไม่คิดเกณฑ์ใหม่: เทียบต้นทุนกับ `ColorGroups` (CREAM 99/RED 149/GREEN 180/PINK 220/BLUE 280/YELLOW 350/GREY 490/PURPLE 790 + BROWN) หาสีที่ตรง/ใกล้สุด **รองรับ `Mix`** ระบุราคาเองได้ (ของ custom/one-off) → ไม่ว่าฐานราคามาจากสี tier หรือ Mix เอง ใช้สูตรคำนวณราคาขายเดียวกันเป๊ะกับระบบปัจจุบัน (`recalcPrices_`) ไม่มีสูตรพิเศษแยก · **3 สถานะ: Yes→"🛒 ซื้อแล้ว"** (เปลี่ยนชื่อ 19 ก.ค. รอบ 2 — จาก "วางแผนจัดซื้อ" เพราะ Yes = ตัดสินใจซื้อแล้วจริงๆ ไม่ใช่แค่วางแผน) **· Hold→เก็บไว้ดูทีหลัง** กดเข้าไปดูใหม่ได้ทุกเมื่อ เลือกต่อเป็น Yes/No ได้ **· No→ปฏิเสธ** ดูประวัติย้อนหลังได้ (ไม่ลบทิ้ง) · **ในหน้า "ซื้อแล้ว" แต่ละรายการกด 👁 View / ✏️ Edit / 🖨 Print ได้** · **🔀 รวม pipeline กับ F4 แล้ว (19 ก.ค.):** เดิม Scout มีคิว "รอคิวลงโค้ด" แยกเป็นของตัวเอง ซ้ำซ้อนกับ F4 Backlog Board ที่ทำหน้าที่เดียวกัน → ตัดคิวแยกทิ้ง เปลี่ยนปุ่มเป็น **"🗃️ นำเข้า Backlog"** ส่งของเข้า Backlog Board โดยตรง (ที่เดียวกับสินค้าที่ซื้อนานแล้วรอลงจริง) ฝั่ง Scout อัปเดตสถานะเป็น `imported` (หายจากลิสต์ซื้อแล้ว) เหตุผล: สินค้าที่จะเข้าระบบมีแค่ 2 ทาง (Scout ของใหม่เล็งเห็นโอกาส / ของซื้อนานแล้วรอลงจริง) แต่ปลายทางหลังซื้อเหมือนกันหมด (ลงต้นทุน/ราคา/รายละเอียด/มอบหมายทีม) ไม่ควรมี 2 ที่เก็บ | ✅ **B3.106/3.95 (19 ก.ค.): ต่อ backend จริงแล้ว + push ขึ้น git แล้ว** — ชีตใหม่ `ScoutSku` (คอลัมน์: scout_id, status, source, date, origin, note, weight, cost_piece, cost_gram, ccy, color, base, price_a, price_b, list_a, list_b, img, created_by, created_at, decided_at) · action ใหม่ใน Code.gs: `scoutList` / `scoutAdd` (รูปอัพขึ้น Drive โฟลเดอร์ `Rainbow Online/Scout/` แบบเดียวกับโมดูลอื่น) / `scoutMove` (เปลี่ยน status — รองรับค่า `imported` โดยไม่ต้องแก้ backend เพิ่ม เพราะไม่ validate ค่า status) / `scoutUpdate` (แก้ไข) — ทุก action เฉพาะ L/A (`canCost_`) · **เสร็จสมบูรณ์: clasp deploy ไปที่ deployment เดิม (AKfycbx3m-Tj... @108) ยืนยันเว็บจริงตอบ build 3.95 แล้ว + Luffy รัน initDrivePermission แล้ว + ทดสอบเพิ่มสินค้าจริงผ่านเว็บจริงแล้ว — ใช้งานได้จริง 100%** · **B3.107 (19 ก.ค. รอบ 2): แก้ rename+รวม backlog แล้ว ทดสอบผ่านเบราว์เซอร์จริงครบ (ชื่อถูกต้อง, ปุ่มนำเข้า Backlog ทำงาน, ข้อมูลย้ายไปที่ Backlog Board ถูกต้อง) — ยังไม่ได้ push (frontend อย่างเดียว ไม่ต้องแตะ Code.gs เพิ่ม)** |
| F2 | 📈 เช็คสินค้าขายดีในไทยตอนนี้ (หมวด Women Fashion & Accessories) | ไม่มี API สาธารณะจาก Shopee/Lazada/TikTok Shop ให้ดึงอัตโนมัติ (scraper ขัด ToS) · เครื่องมือฟรีที่ใช้ได้จริง: **TikTok Creative Center** (creativecenter.tiktok.com — filter ไทย + Fashion Accessories) + **Google Trends** · Shopee/Lazada Seller Center เป็นแค่ analytics ร้านตัวเอง ไม่ใช่เครื่องมือส่องตลาด · สรุป: เป็นรูทีนคนเช็คเอง ระบบแค่ต้องมีที่ให้จดผลไว้ | ⬜ รอ Luffy ตัดสินใจว่าจะทำเป็นรูทีนคน หรือแค่จดในระบบ |
| F3 | 🎬 คอนเทนต์/วิดีโอ — เช็คเทรนด์/ไวรัลในไทย | เครื่องมือเดียวกับ F2 (TikTok Creative Center ตอบได้ทั้งคู่) · ซ้อนกับ **D3** (planner คอนเทนต์ 14 คลิป/สัปดาห์) — ควรรวมเป็นงานเดียวกัน | ⬜ รวมกับ D3 |
| F4 | 🗃️ คิวสินค้าค้าง (Backlog Board) | **🔒 ขอบเขต (เคาะแน่นอนแล้ว 19 ก.ค. รอบ 3): จบที่ Timeline ข้อ 4 "ส่งบัญชี" เท่านั้น** — ห้ามลากยาวไปถึงถ่ายรูป/ลงช่องทาง/คอนเทนต์/วัดผล (ดูตาราง Flow งานเต็มด้านบน) พอ SKU มีต้นทุน+ราคา+ส่งบัญชีแล้ว ออกจาก Backlog ไปเลย ไปอยู่ในคัมภีร์สินค้าตามปกติ · **ปัญหาเดิม:** สินค้า 10-20 อัลบั้มที่ซื้อมาก่อนมี Scout ข้อมูลกระจายไลน์/กระดาษ/สมุด ยังไม่เข้าระบบ + ของใหม่จาก Scout (กด Yes→ซื้อแล้ว→"นำเข้า Backlog") ก็มารวมกันที่นี่เหมือนกัน — 2 ทางเข้า ปลายทางเดียว · ไอคอน 🗃️ (ไม่ใช้ 📦 กันซ้ำกับ In Non-RB) · **🔀 Flow ที่เคาะแล้ว (19 ก.ค. รอบ 3 — แทนที่ดีไซน์กริดเดิมทั้งหมด):** (1) Luffy มอบหมาย SKU ในกองค้างให้ user คนใดคนหนึ่ง (2) user นั้นกรอกเฉพาะฟิลด์บรรยาย **ไม่มีต้นทุน/ราคาเลย**: รูป, หมวด, Class, RBO SKU, ชื่ออัลบั้ม, ชื่อตัวเลือก, รหัส RBWS, น้ำหนัก, Location RBO, Location RBWS, รหัสสี RBWS (ตรงกับฟิลด์ฟอร์ม ➕ Add SKU เดิมเป๊ะ ยกเว้นทุน/ราคา) แล้วกด Save ทีละ SKU (3) Luffy/Assist มารีวิวทีละ SKU ที่กรอกครบ ผ่านแล้วเลือกเส้นทาง **"➕ SKU (บันทึก SKU ใหม่)"** หรือ **"🛍 In Non-RB"** (4) ไปต่อ pipeline บัญชี/ต้นทุนที่มีอยู่แล้วในฟอร์มนั้นๆ ตามปกติ — จบ Backlog Board ตรงนี้ · **มอบหมายงาน:** `assigned_to` ดึงรายชื่อจากชีต Users เดิม (luffy/assist/admina/adminb/graphic/store) — ยังไม่เคาะว่า user ที่ถูกมอบหมายจะเห็นหน้าไหน/กรองยังไง (ถามแล้ว Luffy ยังไม่ตัดสินใจ) · **⛔ กฎต้นทุน (ยืนยันซ้ำ):** ต้นทุนเห็น/แก้ได้เฉพาะ Luffy/Assist เท่านั้น ไม่ว่าจะมอบหมายให้ใคร ต้นทุนใส่ทีหลังตอน Luffy/Assist กด "➕ SKU"/"🛍 In Non-RB" เอง (ฟอร์มปลายทางมีเครื่องมือคำนวณราคาของตัวเองอยู่แล้ว ไม่ต้องสร้างซ้ำใน Backlog) · **❌ ไอเดียที่เสนอแล้วตัดทิ้ง:** เคยเสนอให้เช็ค "ช่องทางการจำหน่าย" (web_status) ตอนรีวิวก่อนส่งต่อ — Luffy บอกงงและสั่ง ignore ไปแล้ว (19 ก.ค.) **อย่าเสนอซ้ำโดยไม่ถามใหม่** | ✅ **B3.108 (19 ก.ค.): สร้างใหม่ทั้งหมดตาม flow รอบ 3 แล้ว push แล้ว** — 4 แท็บ: **📊 ภาพรวม** (กลุ่มตามอัลบั้ม โชว์กรอกอยู่/รอรีวิว/ส่งต่อแล้ว) · **➕ เพิ่มรายการ** (กริดสั้น: อัลบั้ม/ชื่อตัวเลือก/มอบหมาย เท่านั้น ไม่มีทุน/ราคา) · **📝 กรอกรายละเอียด** (การ์ดต่อ SKU กด ✏️ เปิด modal กรอกครบ 11 ช่อง: รูป/หมวด/Class/RBO SKU/อัลบั้ม/ตัวเลือก/RBWS/น้ำหนัก/Location RBO/Location RBWS/สี RBWS — เช็คครบอัตโนมัติ ครบเมื่อไหร่ขยับสถานะเป็น "รอรีวิว" เอง) · **✅ รอรีวิว** (เฉพาะ L/A — เห็นข้อมูลครบของ SKU นั้น กดปุ่ม "➕ ส่งไป SKU ใหม่" หรือ "🛍 ส่งไป In Non-RB" mock ไว้ก่อน) · Scout "นำเข้า Backlog" ปรับ mapping ใหม่แล้ว (เข้าเป็น draft รอกรอกรายละเอียด ไม่พกทุน/ราคาติดมาด้วย) · ทดสอบผ่านเบราว์เซอร์จริงครบ: เพิ่มรายการ→กรอกไม่ครบยังเป็น draft→กรอกครบขยับเป็น ready อัตโนมัติ→รีวิว→ route→overview นับถูก, non-L/A ถูกกันไม่ให้เข้ารีวิวได้, Scout import ได้ shape ใหม่ถูกต้อง · **~~ค้าง: ยังไม่มี backend จริง~~ → ✅ B3.111-B3.112/3.97 (20 ก.ค.): ปุ่ม route เปิดฟอร์มจริง+เติมข้อมูล+รูปแล้ว · backend ชีต Backlog + backlogList/Add/Update/Route เขียนเสร็จ (ดูสรุปจบงาน 20 ก.ค. ด้านบน — รอ Luffy deploy)** · **✅ เคาะแล้ว 19 ก.ค. (รอบ 4): เปิด nav Backlog ให้ทุก role เห็นแล้ว** (ไม่มีต้นทุนอยู่ใน Backlog เลยจึงไม่มีอะไรต้องซ่อน) — พนักงานที่ถูกมอบหมายเข้าแท็บ 📝 กรอกรายละเอียด ได้จริงแล้ว ส่วนแท็บ ✅ รอรีวิว ยังกันด้วย `isLA()` เฉพาะ Luffy/Assist เหมือนเดิม (ทดสอบผ่านเบราว์เซอร์จริงแล้วด้วย role graphic: เห็น Backlog, เพิ่ม/กรอกรายการได้, เข้ารีวิวไม่ได้ — Scout ยังคงซ่อนสำหรับ non-L/A เหมือนเดิมไม่เปลี่ยน) · **🐛 บั๊กที่เจอ+แก้แล้ว (19 ก.ค. รอบ 5, build 3.96/3.96):** ดรอปดาวน์ "มอบหมายให้" เคย hardcode รายชื่อ (Luffy/Assist Manager/Admin A/Admin B/Graphic/User หน้าร้าน WS) ตรงๆ ใน JS — Luffy แก้ชื่อจริงในชีต Users แล้ว (เช่น Admin A → ชื่อพนักงานจริง) แต่ระบบไม่อัปเดตตาม เพราะไม่เคยดึงจากชีตจริงเลย → **แก้แล้ว:** ดึงชื่อจริงผ่าน action `userList` (เดิม master เท่านั้นเรียกได้ ขยายให้ Assist เรียกได้ด้วย) โหลดทุกครั้งที่เปิดแท็บ Backlog (เฉพาะ L/A — role อื่นเห็นแค่ placeholder "ยังไม่มอบหมาย" เพราะ userList ยังจำกัด L/A) ทดสอบยืนยันแล้วว่าเปลี่ยนชื่อในชีต Users แล้วโชว์ถูกต้องในดรอปดาวน์ · **✅ deploy ขึ้นเว็บจริงแล้ว** (clasp push + clasp deploy ไปที่ deployment เดิม @110 ยืนยันเว็บจริงตอบ build 3.96 แล้วด้วย curl) |

---

## 🩷💬 เฟส E — Automation (ดองไว้ รอ routine นิ่ง ตามที่ Luffy สั่ง)
| # | โมดูล | สถานะ |
|---|---|---|
| E1 | 💬 LINE 5 กลุ่ม + Escalation (ค้าง1วัน→เตือนคนทำ · 3วัน→กลุ่มบริหาร) · **เคาะแล้ว 15 ก.ค.: ใช้ LINE OA แพ็กฟรี (300 ข้อความ/เดือน พอสำหรับ ~100) · LINE Notify ปิดบริการแล้ว ต้องใช้ Messaging API · รอ Luffy สมัคร OA + ส่ง Channel Access Token · **แผนเคาะ 16 ก.ค.: reply ฟรีไม่จำกัด→ทำถามตอบได้เต็มที่ · เริ่ม 3 กลุ่ม (เบิก-สต็อก / ปฏิบัติการ RBO / บริหาร) · Low Stock ส่ง digest วันละฉบับ · ตัวนับโควต้าหยุดที่ 280 · ลำดับ L1 โครง+digest → L2 ถามตอบ → L3 สรุปเช้า+escalation → L4 บัญชี/คอนเทนต์+ขาเข้า** | 🔨 L1 เสร็จ B3.84: webhook จับกลุ่ม→LineGroups · เลือกกลุ่มรับแจ้งใน Setting · lineSend_+โควต้าหยุด 280 · digest **14:30 เฉพาะจันทร์–ศุกร์** — **3.91 (17 ก.ค.): พบ trigger 8:30 เก่าค้าง (เด้งผิดเวลา) → setupLineTriggers ลบเก่าทุกตัว+ตั้งใหม่ inTimezone Asia/Bangkok+รายงาน trigger คงเหลือ · เช็ค จ–ศ ในตัว lineDailyDigest_ เอง (trigger ซ้ำ/ผิดยังไงวันหยุดก็เงียบ) · ⚠️ ต้องรัน setupLineTriggers ซ้ำ 1 ครั้งหลัง deploy 3.91** · reply ฟรี · B3.88 แก้โควต้าไม่นับ (คีย์เดือนโดน Date coercion → เติม M นำหน้า) · B3.86: ติ๊กหน้าที่รายกลุ่ม use_stock/use_wd/use_exec/use_content (🎨 คอนเทนต์/กราฟฟิค เพิ่ม 16 ก.ค.) (chips ใน Setting) · digest ส่งทุกกลุ่มที่ติ๊ก 🔔 · จับกลุ่มสำเร็จแล้ว 16 ก.ค. · **L2 เสร็จ B3.89: ถามตอบในกลุ่ม เช็ค<คำค้น>/เบิก/ช่วย (reply ฟรี · cost ไม่ตอบ · ข้อความอื่นเงียบ)** → ต่อ L3 สรุปเช้า+escalation |
| E2 | 🩷 คอนเทนต์+AI + n8n ลงโพสต์อัตโนมัติ | ⏸ |

## 📊 กองแยก — ไม่ใช่โค้ดเว็บ
| # | งาน | สถานะ |
|---|---|---|
| X1 | วิเคราะห์ยอดขาย Shopee (ไฟล์ Order+Income ม.ค.–พ.ค. 69 อยู่ใน project แล้ว 10 ไฟล์) | ⬜ รอสั่ง |
| X2 | LJ sub-brand (แพ็กเกจหรู Moissanite) | ⬜ |
| X3 | Tagline Rainbow Online ("wear your vibe" ยังไม่เคาะ) | ⬜ |
| X4 | **แผนแก้คอขวด "แบ่งงานไม่ลง" (21 ก.ค.)**: แยกงาน 4 กอง (ปล่อยเลย/ปล่อยพร้อมกฎ/เก็บไว้แต่จด/เทอเท่านั้น) · F5 คือเครื่องมือหลัก — งานไปกองที่ขั้นที่มีเจ้าของ ไม่กองที่ Luffy · ก้าวต่อไป: เปิด `edit_fields_admin` เพิ่มให้ Admin เคลียร์คิว incomplete SKU เอง (ราคา/ทุนล็อกเหมือนเดิม) + SOP ให้ Thanya | 🔶 เริ่มแล้ว |

---

## ลำดับที่ตกลง: A → B → C → D → E
เหตุผล: A เร็วเคลียร์หนี้เก่า · B คือของที่ธุรกิจต้องใช้จริงก่อนออกแฟร์ · C ต่อยอด B (สต๊อกแฟร์ต้องนิ่งก่อน sync) · D อิสระ แทรกได้ · E รอ routine นิ่ง

## ⚠️ กติกาทำงาน (ห้ามลืม)
- BUILD stamp 3 จุดตรงกัน (BUILD / FRONT_VER / doGet) ก่อนส่งทุกครั้ง
- `node --check` ผ่านก่อนส่ง + เช็คฟังก์ชันซ้ำ = 1 ทุกตัว
- ส่งไฟล์พร้อม snippet เช็คเวอร์ชัน F12
- แก้ผ่าน Python · อ่านโค้ดเดิมก่อนแก้ · cp แยกคำสั่ง ไม่ chain &&
- Deploy: Edit deployment → New version เท่านั้น · HTML อัพผ่าน GitHub Upload files
- CSV บัญชี: คอลัมน์ล็อกถาวร ห้ามมี comma ในตัวเลข
- เปิดเว็บจริง https://luffy108269.github.io/... เท่านั้น (ห้ามเปิด file:// — เจอบทเรียนแล้ว 15 ก.ค.)
- **ไฟล์ frontend ส่งชื่อ `index.html` เสมอ — ห้ามใช้ index_build2.html (Luffy อัพขึ้น GitHub Pages ตรงๆ ไม่ต้องแก้ชื่อ · กำหนด 17 ก.ค.)**
