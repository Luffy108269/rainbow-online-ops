# RB Online Ops — Master Tracker
> อัปเดตล่าสุด: 21 ก.ค. 2569 · Build: B3.113 (html) / 3.97 (gs)
> **B3.113 (21 ก.ค.):** ถอดป้าย "🧪 ตัวอย่าง UI mock" ที่ค้างอยู่หน้า Backlog (ต่อชีตจริงแล้วตั้งแต่ B3.112) — เหลือ hint ขอบเขตสั้นๆ แทน · error "ไม่รู้จักคำสั่ง backlogList" บนเว็บจริง = backend ยังเป็น 3.96 **รอ Luffy วาง Code.gs 3.97 + deploy + รัน initDrivePermission** (ขั้นตอนอยู่ในสรุป 20 ก.ค. ด้านล่าง)

## 🔗 งานผูกรหัสหมู่ 21 ก.ค. 2569 — massBind217 (เขียนเสร็จ รอรัน)
- **โจทย์:** Luffy ค้นบันทึกเก่าเจอ mapping RBO SKU ↔ RBWS SKU **739 รายการ** (ไฟล์ `mass_21.7 ให้claudeทำไฟล์1.xlsx` แท็บ Sheet1: RBO/ชื่ออัลบั้ม/ชื่อตัวเลือก/RBWS · ตรวจแล้วสะอาด: RBO ไม่ซ้ำ ไม่มี RBO→หลาย WS · 295 WS ไม่ซ้ำ = หลายไซส์/สีใช้ WS เดียวกันตามกติกา · 41 อัลบั้ม) → เอาเข้าเว็บ: ผูก WS + ใส่ชื่ออัลบั้ม/ชื่อตัวเลือก
- **กติกาที่เคาะ:** RBO มีในคัมภีร์แล้ว = ยึดตัวเดิม+ผูก WS+**อัปเดตชื่ออัลบั้ม/ตัวเลือกตามไฟล์เสมอ**+เติมช่องว่างจาก PendingSku · ไม่มี = สร้างใหม่ (qty 0 เหมือน pendBind) · ตัวเดิมผูก WS อื่น = ไม่ทับ WS แต่ชื่อยังอัปเดต (รายงาน conflict) · ไม่ลบแถว PendingSku
- **ทำแล้ว:** ฟังก์ชัน run-ครั้งเดียว `massBind217Preview` (ดูสรุปก่อน ไม่เขียน) + `massBind217` (เขียนจริง) + ข้อมูล 739 แถวฝังใน Code.gs ท้ายไฟล์ (แบบเดียวกับ importSeed) · เขียนชีตแบบ bulk 2 ครั้งจบ กัน timeout · เทียบรหัส trim+case-insensitive · ทดสอบ logic ผ่าน mock ครบ 4 เคสในเบราว์เซอร์แล้ว · **รันเสร็จแล้วลบท่อน MB_ALBUMS/MB_ROWS/ฟังก์ชัน ออกจาก Code.gs ได้เลย**
- **⚠️ ขั้นตอน Luffy:** (1) วาง Code.gs ใหม่ (รวม Backlog 3.97 + massBind217 ในไฟล์เดียว) → Deploy → Edit → New version (2) รัน initDrivePermission (3) **Run ▶ massBind217Preview ดูตัวเลขก่อน** (4) ถูกใจแล้ว Run ▶ massBind217 · ชีต 'ยังไม่มีrbws' ในไฟล์ Excel (1,142 รายการยังหา WS ไม่เจอ) ไม่เกี่ยวกับรอบนี้ — ไว้หาเพิ่มแล้วค่อยทำรอบใหม่
> **กติกา: จบงานทุกครั้ง Claude ต้องอัปเดตไฟล์นี้ก่อน push git**

## 🌙 จบงาน 20 ก.ค. 2569 — สรุปวันนี้
- **F4 Backlog — ปุ่ม "ส่งไป SKU ใหม่/In Non-RB"** ✅ เลิก mock แล้ว (B3.111) — กดแล้วพาไปหน้า ➕ SKU หรือ 🛍 In Non-RB จริง เปิดโหมด New SKU ให้เอง เติมข้อมูลจาก Backlog ให้ครบ (หมวด/Class/รหัส/อัลบั้ม/ตัวเลือก/น้ำหนัก/Location · ฝั่ง SKU ใหม่เติม RBWS+Location RBWS+สี RBWS ด้วย) **+ แนบรูปที่ถ่ายไว้ตอน Backlog ให้อัตโนมัติ**
- **F4 Backlog — backend จริง** ✅ เขียนแล้ว (B3.112/3.97) — Luffy ได้ส่ง Code.gs ตัวจริงมาให้ (เก็บสำเนาไว้ที่ `apps-script/Code.gs` ในเครื่อง — โฟลเดอร์นี้ `.gitignore` กันไว้ ไม่ขึ้น repo public):
  - **Code.gs (3.97):** ชีตใหม่ `Backlog` (bl_id/status/album/variant/assigned_to/category/cls/sku_rbo/rbws_code/weight/loc_rbo/loc_rbws/rbws_color/img/created_by/created_at/updated_at/routed_at) · action ใหม่ 4 ตัวตามแพทเทิร์น Scout: `backlogList` (ทุก role) / `backlogAdd` (batch จากกริด+Scout import · เลขรัน BL ผ่าน nextDoc_) / `backlogUpdate` (กรอกรายละเอียด · รูปอัพ Drive โฟลเดอร์ `Rainbow Online/Backlog/` · เช็คครบ 11 ช่องฝั่ง server → ready/draft เอง · กันแก้ตัวที่ส่งต่อแล้ว · assigned_to แก้ได้เฉพาะ L/A) / `backlogRoute` (L/A เท่านั้น → sent_sku/sent_nonrb) · แถม: `extCreate` รองรับ `it.img_url` (รูปจาก Backlog ส่ง URL ตรง ไม่อัพซ้ำ — แบบเดียวกับ importExistSku)
  - **index.html (B3.112):** Backlog โหลด/บันทึกผ่านชีตจริงทุกแท็บ — **ข้อมูลไม่หายเมื่อรีเฟรชแล้ว** · Scout "นำเข้า Backlog" ลงชีตจริง · route แล้วรูปส่งต่อเป็น URL (addSku ผ่าน `q.img` ที่ backend มีอยู่แล้ว / In Non-RB ผ่าน `img_url` ใหม่)
  - **ทดสอบผ่านเบราว์เซอร์แล้ว** (stub API จำลอง backend เพราะเครื่องนี้ deploy ไม่ได้): เพิ่มจากกริด→ได้ bl_id จริง / กรอกครบ→ready อัตโนมัติ / ล้าง cache แล้ว blLoad คืนข้อมูลครบ (จำลองรีเฟรช) / route ทั้ง 2 เส้นทาง prefill ถูก+รูป URL ติดไป / ebnAdd พก img_url เข้าบิล / Scout import ลงชีต · `new Function(Code.gs)` syntax ผ่าน · ไม่มีฟังก์ชันซ้ำ · console ไม่มี error
  - **⚠️ รอ Luffy ทำ 2 ขั้นก่อนใช้จริง:** (1) วาง Code.gs ใหม่ทับใน Apps Script → Deploy → Manage deployments → ✏️ Edit → New version → Deploy (2) รัน `initDrivePermission` ซ้ำ 1 ครั้งกันเงียบ (มีโฟลเดอร์ Drive ใหม่ `Backlog`) — ชีต Backlog สร้างเองอัตโนมัติตอนเปิดแท็บครั้งแรก · **ห้าม refresh เว็บก่อน deploy backend** ไม่งั้น sysCheck จะเตือนเวอร์ชันไม่ตรง (frontend 3.97 / backend 3.96)
- **ค้างต่อ:** D3 (ถ่ายรูป+ลงช่องทาง+คอนเทนต์) และ "วัดผล" ยังเป็นแค่ concept ในตาราง Flow ด้านล่าง ยังไม่เริ่มสร้าง

---

## ✅ เสร็จแล้ว (ยืนยันจากโค้ดจริง)
| โมดูล | หมายเหตุ |
|---|---|
| 🔵 ของเข้า 2 ทาง | แท็บ เบิก (4 สถานะ) + In Non-RB |
| 🟣 คัมภีร์สินค้า | ครบ · **B3.99: สิทธิ์แก้ต่อ role ย้ายไปชีต Config แถว `edit_fields_admin` / `edit_fields_graphic` / `edit_fields_storefront` (คั่น comma · default img,loc_rbws,loc_rbo = เท่าของเดิม) — Luffy เปิด/ปิดเองได้เพื่อกระจายงานให้พนักงานเติมข้อมูล · ⛔ ล็อกตาย L/A เสมอไม่ว่า Config เขียนอะไร: cost_latest, cost_note, color_rbo, price ทั้ง 5 (SKU_LOCKED_) · บังคับ 2 ชั้น หน้าจอ+backend (skuUpdate กรอง ALLOW+imgOK) · ไม่มีสิทธิ์สักช่อง = ไม่โชว์ปุ่ม Edit · ติ๊กช่องทางขายเปิดให้พนักงานได้ผ่าน web_status** · **B3.100: ชีต Selections เพิ่ม 3 คอลัมน์ (ensureHeaders_ สร้างให้เองตอนเปิดเว็บ): `category` (ประเภท — ว่าง=ถอดจาก prefix อัตโนมัติ กรอกทับได้ทั้งในชีตและหน้า Edit dropdown) · `price_rec` (Recommended price ตัวเลข ล็อก L/A) · `tags` (คำค้นอิสระ คั่น , — ช่องค้นหาเจอด้วย) · ช่องทางขายแก้ในชีตได้ที่คอลัมน์ `web_status` เดิม (ชื่อช่องทางคั่น , เช่น Ketshop,Shopee) · prefix `FB`=Bracelet (แก้บั๊ก FB-BRx ขึ้น Non-Silver) · การ์ดสินค้ามีป้ายประเภทข้าง SKU · ปุ่ม ⚠️ รายละเอียดไม่ครบ ย้ายขึ้นบนข้างหัวข้อ** · **B3.101: แถวจุดสีราคา Mix(สายรุ้ง)→Brown ใต้ chips ช่องทาง (หน้าตาเดียวกับหน้าแรก · toggle · วงแหวนชมพูตอน active) · กรอง 4 ชั้นพร้อมกัน: ค้นหา+ประเภท+ช่องทาง+สี (เช่น แมว+Earrings+Lazada+Brown) · ถอดกฎ B3.60 พิมพ์แล้วล้างสีออกแล้ว — จุดสีหน้าแรก (catColorGo) ยังล้างค้นหา+สลับแท็บเหมือนเดิม · tags เว้นวรรครอบ comma ได้อิสระ (ค้นแบบ substring)** · **B3.102/3.90: เมนูชีต 💰 RB ราคา → 🏷 เติมประเภทสินค้า (menuFillCategory) เติม category เฉพาะช่องว่างทั้งชีตรวดเดียว (ค่าที่พิมพ์เองไว้ไม่แตะ · ตรรกะ skuTypeGs_ ตรงกับหน้าเว็บรวม FB=Bracelet · ไม่เข้าหมวด=Non-Silver)** · **B3.97 (17 ก.ค.): ค้นหา 4 ช่อง (SKU RBO/RBWS/อัลบั้ม/ชื่อตัวเลือก · partial+case-insensitive) · chips กรองประเภทสินค้า 8 หมวด+Non-Silver (ถอดจาก prefix SKU อัตโนมัติ longest-match — BAB/BAS ก่อน BA · **B3.98: Non-Silver = ของที่ไม่ใช่เครื่องประดับเลย (prefix ไม่เข้า 8 หมวด) — EB/ES/RB/BAB ยังอยู่หมวดเครื่องประดับตามเดิม → ทุก SKU มีประเภทเสมอ**) · chips ช่องทางขาย 11 ช่อง+ตัวเลขนับ (ทุก role) · chip ⚠️ SKU รายละเอียดไม่ครบ (L/A) โชว์ว่าขาดช่องไหนบนการ์ด · ฟิลด์เช็คอยู่ชีต Config แถว `bible_check_fields` (สร้างแถวให้อัตโนมัติผ่าน cfgEnsure_) default: pack_class, color_rbo\|price_base, sku_rbo, album_id, sel_name, cost_latest, weight, img · กติกา: comma=ต้องมีทุกตัว · \|=มีอย่างใดอย่างหนึ่งก็ครบ · ค่าว่างหรือ 0 = ยังไม่กรอก** |
| 🟡 ราคา 5 ตัวเลข | ชุด A/B/C + ปุ่มปรับราคาทั้งระบบ + เมนู 💰 ในชีต |
| 🟠 บัญชี 2 บริษัท | Etax csv + พี่ตุ่น csv + Expense ครบ (Edit บิลได้) |
| Ketshop Queue | รับของ→คิว→PDF→ยืนยัน→ประวัติ |
| เช็คสต็อก (นับ) | โหมดร้าน/แฟร์ · แก้ inline · Export .xlsx · **B3.79: การ์ดแนวตั้งจบจอเดียว (เลิกตารางกว้าง) + 🖨️ ใบเช็คสต็อก PDF เรียงตาม Location ช่อง "นับได้" เว้นว่างให้พนักงานเขียน (blind count) · B3.81: ฝังรูปเสมอ (Drive thumbnail sz=w96 ~3KB/รูป · fetchAll ก้อนละ 100 · stockPdf เข้า HEAVY 4 นาที) + B3.82: คอลัมน์ ชื่ออัลบั้ม/ชื่อตัวเลือก มีหัวตาราง · Location ป้ายข้าง SKU · thead ซ้ำทุกหน้า (แบ่งกันนับได้) · เลือกบาง SKU มี autocomplete รูปจาก CAT · B3.85: โฟลเดอร์ เช็คสต็อก แชร์ anyone-with-link — ไม่ต้องขอสิทธิ์รายใบ · เซฟ Drive: Rainbow Online/เช็คสต็อก/** |
| นำเข้า SKU เดิม | RBWS/ภายนอก ไม่แตะบัญชี · **16 ก.ค.: นำเข้า RBWS 497 SKU จาก 82 ไฟล์ catalogue (.xls มี merge cell+รูปฝัง) → PendingSku รอผูกรหัส RBO · B3.90 แก้: importData เติมรูปให้ pending เดิมที่ยังไม่มีรูปตอนอัพซ้ำ + image_base64 ต้องมีหัว data:image/jpeg;base64, · B3.91 แก้ timeout เติมรูป (อ่านชีตครั้งเดียว เขียนตรงเซลล์) ไฟล์นำเข้าซอย 50 SKU/ไฟล์ · B3.92: ฟอร์มนำเข้า SKU (RBWS) พิมพ์รหัส RBWS ≥3 ตัว → autocomplete จาก PendingSku เติมข้อมูล+รูปเดิมให้ (img_url ไม่อัพรูปซ้ำ) · กติกา: RBWS ซ้ำได้หลาย RBO · RBO ห้ามซ้ำ (backend เช็คอยู่แล้ว) · หลายไซส์/สี = RBO คนละตัว RBWS เดียวกัน · B3.93 แก้ชิปช่องทางในฟอร์มกดไม่ได้ (บั๊กเก่า B3.57: clickable=false ทำ onclick ไม่ถูกสร้าง) + save error เด้ง toast · B3.94: ฟังก์ชัน g2 หายทั้งไฟล์ (ฟอร์มเรียก 20+ จุด → Save/hint ตายเงียบ) ใส่คืนแล้ว — บทเรียน: health check ควรเช็คฟังก์ชันที่ถูกเรียกแต่ไม่มีตัวตนด้วย · B3.95: การ์ดรอรหัสโชว์เฉพาะตัวยังไม่ผูก (pendingList แนบ used) ฟอร์มยังค้นตัวผูกแล้วได้พร้อมป้าย ✓ · **B3.104/3.92: ⚡ ผูกรหัสด่วนที่การ์ดรอรหัสหน้าแรก — ช่องค้นหา (RBWS/รายละเอียด · โชว์ 15 ปกติ/50 ตอนค้น) + dropdown เลือก prefix ครั้งเดียว (24 prefix มีชื่อไทย) → กด ⚡ ทีละการ์ด เลขรันอัตโนมัติผ่าน nextSkuByPrefix_ (แยกจาก nextSku_ · เคารพ sku_start_XX ใน Config) หรือพิมพ์รหัสเองในช่องก่อนกด · backend action pendBind: copy PendingSku→Selections (detail/weight/color_rbws/loc_rbws/img/cost→cost_latest · qty_shop=0) เช็ครหัสซ้ำ · ผูกแล้วการ์ดหาย+คัมภีร์รีเฟรชทันที · เหตุผล: Ketshop ไม่มีข้อมูล RBWS การผูกต้องเกิดที่ระบบเรา แล้วเอารหัส RBO ไปลง Ketshop · รายละเอียดที่เหลือเติมทีหลังผ่านปุ่ม ⚠️ ไม่ครบ** · **B3.105/3.93: ของเก่าส่วนใหญ่มีรหัส RBO อยู่แล้วฟอร์แมตอิสระ (RB009RT-ขาว / A016แบบที่5 / หยดน้ำ5X8#สีเหลือง) → ช่องรหัสบนการ์ดเป็น autocomplete ค้นคัมภีร์ (sku_rbo/album_id/sel_name/detail · ≥2 ตัวอักษร · โชว์ 6 ตัว · ป้ายแดงถ้าตัวนั้นมี WS แล้ว) · pendBind ฉลาด: รหัสมีอยู่ในคัมภีร์=ผูกเข้าตัวเดิม (เติม sku_rbws + เติมเฉพาะช่องว่าง detail/weight/color_rbws/loc_rbws/cost_latest/img — ไม่ทับของเดิม · ถ้าตัวเดิมผูก WS อื่นอยู่ = error บอก) · ไม่มี=สร้างใหม่ · เลิกบังคับตัวใหญ่ เทียบ case-insensitive รหัสไทยได้ · toast แยกโหมด 🔗 ผูกตัวเดิม / ✓ สร้างใหม่** |
| ~~R4 แก้ qty หลังรับของ~~ | **ตัดสินใจ+ทำแล้ว** ตั้งแต่ B3.5x: qty ล็อกถาวรหลัง received แก้ได้เฉพาะ นน.ชั่งจริง (`wdEditReceived`) |
| 🔒 Security | **B3.94/3.94 (18 ก.ค.): ปิดช่องโหว่ `?dbg=exp` ใน `doGet`** — เดิมใครก็ยิง URL เว็บแอป + `?dbg=exp` ได้โดยไม่ต้อง login แล้วโค้ดจะเขียนจริง (`PING` test) + อ่านข้อมูลจริงจากชีต Expenses (headers, จำนวนแถว, ยอด/เดือน, แถวแรก) ออกมา ตอนนี้ `doGet` ตอบแค่ `{ok,system,build,time}` เสมอ ไม่แตะชีตใดๆ · เจอระหว่างสแกน repo หา token/secret หลุด 18 ก.ค. · ยืนยันแล้วบนเว็บจริงหลัง deploy: `?dbg=exp` คืนแค่ status ปกติ build 3.94 |

---

## 🔒 Security — ค้าง (เจอตอนสแกนหา token/secret หลุด 18 ก.ค. 2569 — ยังไม่จัดลำดับเข้าเฟส)
| # | งาน | หมายเหตุ | สถานะ |
|---|---|---|---|
| S1 | Session token แยกจาก password hash | ตอนนี้ `key` ที่ frontend เก็บไว้ = `x.passhash` ตรงๆ (`verify_` เทียบ `passhash===k` ตรงๆ) ไม่มี session แยก ไม่มีวันหมดอายุ ไม่มี revoke รายอุปกรณ์ — ถ้า `key` หลุด (localStorage/log/MITM) = เข้าถาวรจนกว่าเจ้าของจะเปลี่ยนรหัสผ่าน | ⬜ |
| S2 | Hash แข็งขึ้น + rate limit ที่ login | `hash_()` ใช้ SHA-256(password+salt ตายตัว `'RBOsalt'`) เป็น fast hash ไม่มี per-user random salt · รหัสผ่านขั้นต่ำแค่ 4 ตัว → ถ้าชีต Users หลุด brute-force ได้เร็ว · ไม่มี rate limit/lockout ที่ `action:'login'` เลย ถูกสุ่มรหัสผ่านไม่จำกัดครั้งได้ | ⬜ |

### 📝 บันทึกเพิ่ม (18 ก.ค. 2569): TOKEN_SECRET เก่าใน git history
- **repo `re-order`** (คนละ repo กับ rainbow-online-ops นี้) มี git history เก่า commit `d23c331` ที่เคยมี `TOKEN_SECRET` ฝังอยู่
- **rotate แล้ว** — token ตัวนั้นใช้ไม่ได้แล้ว (inert) ต่อให้มีคนขุด history เจอก็เอาไปใช้อะไรไม่ได้
- **ตัดสินใจ:** ปล่อย history ของ repo `re-order` ไว้แบบเดิม ไม่ล้าง/ไม่ rewrite history เพราะความเสี่ยงจริง = 0 (token inert แล้ว) — การ rewrite history มี cost/risk สูงกว่าประโยชน์ที่ได้
- repo นี้ (`rainbow-online-ops`) ยังคงเป็น **public** ต่อไป เพราะเป็นเว็บใช้งานจริงผ่าน GitHub Pages (Pages แบบ custom domain/private ต้องใช้ GitHub Pro) — ถ้าจะเปลี่ยนเป็น private ต้องอัปเกรดแผนก่อน

## 🔥 เฟส A — เก็บกวาด + ของค้างชิ้นเล็ก (ทำรวดเดียว 1 build)
| # | งาน | ขนาด | สถานะ |
|---|---|---|---|
| A1 | ล้างไฟล์ Excel เล่มผีใน Drive (ul-NaN/Wed/Tue) — เขียนสคริปต์สแกน+ลิสต์ให้ดูก่อนลบ | S | ⬜ |
| A2 | PDF แยกตอน Assist ยืนยันรับของ (`wdReceive` ยังไม่ gen PDF) | S | ⬜ |
| A3 | แท็บประวัติบัญชีย้อนหลังใน UI | M | ⬜ |
| A4 | หน้าดู log (backend `log_()` มีแล้ว ขาดแค่จอ) | S | ⬜ |

## 🔴 เฟส B — ออกงานแฟร์ (โมดูลใหญ่สุด · 7 ขั้น)
| # | ขั้น | สถานะ |
|---|---|---|
| B1 | สร้างรอบแฟร์ + เคาะราคาสีประจำรอบ | ⬜ |
| B2 | ตั้งโควต้า สี×หมวด×จำนวนแบบ (ดึงเฉพาะมีสต๊อก) | ⬜ |
| B3 | Picking List PDF มีรูป + Admin ติ๊กเก็บของ | ⬜ |
| B4 | ยกออก: สต๊อกร้าน → สต๊อกแฟร์ (เช่น 100→แฟร์5/ร้าน95) | ⬜ |
| B5 | ขายหน้างานผ่าน POS (Ketshop — นอกระบบเรา บันทึกยอดเข้า) | ⬜ |
| B6 | นับจริง เทียบ ควรเหลือ=ยกไป−POS เขียว/แดงรายตัว | ⬜ |
| B7 | เคลียร์ส่วนต่าง+หมายเหตุ รับกลับ ปิดรอบ สรุปต่อสี/หมวด | ⬜ |

## ⚪ เฟส C — สต๊อกอัจฉริยะ (ครึ่งหลังของโมดูลสต๊อก)
| # | งาน | สถานะ |
|---|---|---|
| C1 | นำเข้า snapshot Ketshop เป็นฐาน ("Ketshop เป็นนาย" — เคาะแนวทางแล้ว) | ⬜ |
| C2 | Sync สต๊อกสองทางกับ Ketshop | ⬜ |
| C3 | อ่านเมล KETSHOP Low Stock อัตโนมัติ | ✅ B3.75: สแกนรายชั่วโมง → LowStockAlerts → การ์ดหน้าแรก · เข้าเงื่อนไขเฉพาะ <5 (Config `ls_threshold`) · เติมของ ≥5 ปิดอัตโนมัติ · คลังร่วม 1 รายการ/SKU · กันซ้ำ `ls_last_scan` · **parser: เมล Ketshop = HTML ปนใน plain + `<td>` คั่นด้วย newline → ต้องยุบ newline ก่อนแปลง `</tr>`=บรรทัด `</td>`=tab (ทดสอบผ่านกับเมลจริง 15 ก.ค.)** · แก้ parser เมื่อไหร่รัน `lsRescan` · **B3.78: การ์ดมีปุ่ม 📝 เบิก (ลง Draft + ปิดรายการ note "สั่งเบิกแล้ว") / ➕ ผูกรหัส (เปิดแท็บ add เติมรหัสให้ · L/A เท่านั้น)** |
| C4 | คิวเช็คเติมของเทียบเป้าตามดาว · ใกล้ 0 เด้งทันที | ⬜ |
| C5 | ประวัตินับ → อัตราไหล → แนะนำเบิก/เลื่อนดาว | ⬜ |
| C6 | รอบนับใหญ่รายไตรมาส | ⬜ |

## 🟢🟤↩️ เฟส D — โมดูลปฏิบัติการที่เหลือ
| # | โมดูล | สถานะ |
|---|---|---|
| D1 | 🟤 สิ้นเปลือง/แพ็คเกจจิ้ง: รับเข้า→บันทึกเช็ค→อัตราใช้/วัน→เตือนซื้อ | ⬜ |
| D2 | ↩️ ตีกลับ/คืน/เปลี่ยน: แจ้งเคส→เช็คสภาพ→ดี=เข้าสต๊อก / ตำหนิ=แยก | ⬜ |
| D3 | 🟢 Launch checklist + Planner รายเดือน เขียว/เหลือง/แดง · **16 ก.ค.: Luffy บอกงานค้างที่ตัวเองเยอะ — ยกความสำคัญขึ้น: pipeline ลงสินค้าใหม่ (ถ่ายรูป→ราคา→ลง Ketshop→ลงช่องทาง→เปิดขาย มอบหมายรายขั้น) + planner คอนเทนต์ 14 คลิป/สัปดาห์** | ⬜ |

## 🆕 ฟีเจอร์ใหม่ที่คุยไว้ 18-19 ก.ค. 2569 — ยังไม่เข้าคิว
> บันทึกไว้กันหาย เพราะคุยยาวใน chat แล้ว context จะหายตอนแชทใหม่ — รายละเอียดสเปกเต็มอยู่ในนี้แล้ว พร้อมเริ่มออกแบบ/สร้างได้เลยเมื่อสั่ง

### 🗺️ Flow งานเต็ม — Timeline 11 ขั้น (Luffy วางไว้ 19 ก.ค. รอบ 3 — เคาะเขตแดนแล้ว กันงง/กันปนกัน)
> Luffy ให้ timeline เต็ม 11 ขั้น แล้วถามเองว่า "จะปนกันไหม" — เคาะแล้วว่า**ไม่ปน** ถ้าตัดเป็น 3 ช่วงชัดๆ ส่งต่อกันเป็นทอดๆ (ห้ามยัดรวมเป็นฟีเจอร์เดียว):

| ช่วง | Timeline ข้อ | โมดูลรับผิดชอบ |
|---|---|---|
| **① เข้าระบบ** | 1.สินค้ามา · 2.ทำต้นทุนได้ราคาได้ SKU · 3.ได้รายละเอียดพร้อมลงช่องทาง · 4.ส่งบัญชี | **F4 Backlog Board** — **จบที่ข้อ 4 (ส่งบัญชี) เท่านั้น** พอมี SKU+ต้นทุนแล้วออกจาก Backlog ไปเลย ไม่ต้องแบกต่อ |
| **② คอนเทนต์** | 5.ถ่ายรูป 10 รูป+ปก · 6.รายละเอียดสินค้าแบบยาว · 7.ลงสินค้าตามช่องทางที่วางไว้ · 8.Video แผนลงคอนเทนต์ | **D3** เดิม (เฟส D ด้านล่าง — Luffy ยกความสำคัญไว้แล้ว 16 ก.ค.) |
| **③ วัดผล** | 9.ติดตามผล · 10.ถ้าขายไม่ได้ทำยังไง · 11.Up sell | **ช่องว่างใหม่ ยังไม่สร้าง** — ยังไม่เคาะรายละเอียดว่าวัด/ทำยังไง รอ Luffy คิดต่อ |

**เหตุผลที่แยก:** ยัดทุกอย่างเข้า Backlog Board ตัวเดียวจะกลายเป็นฟีเจอร์ยักษ์ทำไม่เสร็จสักที แยกเป็น 3 ช่วงเล็กสร้างเสร็จไวกว่าและใช้จริงได้เร็วกว่า

| # | ฟีเจอร์ | สเปก/หมายเหตุ | สถานะ |
|---|---|---|---|
| F1 | 🔎 Scout หา New SKU จากซัพพลายเออร์ | Luffy หรือ Assist manager (role L/A) เป็นคนเจอสินค้าแล้วแอดเข้าระบบเอง (ไม่ใช่ auto-scrape) · ฟิลด์: แหล่งที่มา, วันที่/เวลา, รูปภาพ, น้ำหนัก, ต้นทุนต่อชิ้น, ต้นทุนต่อกรัม, สกุลเงิน (+อัตราแลกเปลี่ยนถ้าไม่ใช่ THB), ในไทย/ต่างประเทศ, **หมายเหตุ** (เพิ่ม 19 ก.ค.) · ใส่ต้นทุนแล้ว**ระบบแนะนำสีราคาให้อัตโนมัติ** ใช้ของเดิมทุกอย่าง ไม่คิดเกณฑ์ใหม่: เทียบต้นทุนกับ `ColorGroups` (CREAM 99/RED 149/GREEN 180/PINK 220/BLUE 280/YELLOW 350/GREY 490/PURPLE 790 + BROWN) หาสีที่ตรง/ใกล้สุด **รองรับ `Mix`** ระบุราคาเองได้ (ของ custom/one-off) → ไม่ว่าฐานราคามาจากสี tier หรือ Mix เอง ใช้สูตรคำนวณราคาขายเดียวกันเป๊ะกับระบบปัจจุบัน (`recalcPrices_`) ไม่มีสูตรพิเศษแยก · **3 สถานะ: Yes→"🛒 ซื้อแล้ว"** (เปลี่ยนชื่อ 19 ก.ค. รอบ 2 — จาก "วางแผนจัดซื้อ" เพราะ Yes = ตัดสินใจซื้อแล้วจริงๆ ไม่ใช่แค่วางแผน) **· Hold→เก็บไว้ดูทีหลัง** กดเข้าไปดูใหม่ได้ทุกเมื่อ เลือกต่อเป็น Yes/No ได้ **· No→ปฏิเสธ** ดูประวัติย้อนหลังได้ (ไม่ลบทิ้ง) · **ในหน้า "ซื้อแล้ว" แต่ละรายการกด 👁 View / ✏️ Edit / 🖨 Print ได้** · **🔀 รวม pipeline กับ F4 แล้ว (19 ก.ค.):** เดิม Scout มีคิว "รอคิวลงโค้ด" แยกเป็นของตัวเอง ซ้ำซ้อนกับ F4 Backlog Board ที่ทำหน้าที่เดียวกัน → ตัดคิวแยกทิ้ง เปลี่ยนปุ่มเป็น **"🗃️ นำเข้า Backlog"** ส่งของเข้า Backlog Board โดยตรง (ที่เดียวกับสินค้าที่ซื้อนานแล้วรอลงจริง) ฝั่ง Scout อัปเดตสถานะเป็น `imported` (หายจากลิสต์ซื้อแล้ว) เหตุผล: สินค้าที่จะเข้าระบบมีแค่ 2 ทาง (Scout ของใหม่เล็งเห็นโอกาส / ของซื้อนานแล้วรอลงจริง) แต่ปลายทางหลังซื้อเหมือนกันหมด (ลงต้นทุน/ราคา/รายละเอียด/มอบหมายทีม) ไม่ควรมี 2 ที่เก็บ | ✅ **B3.106/3.95 (19 ก.ค.): ต่อ backend จริงแล้ว + push ขึ้น git แล้ว** — ชีตใหม่ `ScoutSku` (คอลัมน์: scout_id, status, source, date, origin, note, weight, cost_piece, cost_gram, ccy, color, base, price_a, price_b, list_a, list_b, img, created_by, created_at, decided_at) · action ใหม่ใน Code.gs: `scoutList` / `scoutAdd` (รูปอัพขึ้น Drive โฟลเดอร์ `Rainbow Online/Scout/` แบบเดียวกับโมดูลอื่น) / `scoutMove` (เปลี่ยน status — รองรับค่า `imported` โดยไม่ต้องแก้ backend เพิ่ม เพราะไม่ validate ค่า status) / `scoutUpdate` (แก้ไข) — ทุก action เฉพาะ L/A (`canCost_`) · **เสร็จสมบูรณ์: clasp deploy ไปที่ deployment เดิม (AKfycbx3m-Tj... @108) ยืนยันเว็บจริงตอบ build 3.95 แล้ว + Luffy รัน initDrivePermission แล้ว + ทดสอบเพิ่มสินค้าจริงผ่านเว็บจริงแล้ว — ใช้งานได้จริง 100%** · **B3.107 (19 ก.ค. รอบ 2): แก้ rename+รวม backlog แล้ว ทดสอบผ่านเบราว์เซอร์จริงครบ (ชื่อถูกต้อง, ปุ่มนำเข้า Backlog ทำงาน, ข้อมูลย้ายไปที่ Backlog Board ถูกต้อง) — ยังไม่ได้ push (frontend อย่างเดียว ไม่ต้องแตะ Code.gs เพิ่ม)** |
| F2 | 📈 เช็คสินค้าขายดีในไทยตอนนี้ (หมวด Women Fashion & Accessories) | ไม่มี API สาธารณะจาก Shopee/Lazada/TikTok Shop ให้ดึงอัตโนมัติ (scraper ขัด ToS) · เครื่องมือฟรีที่ใช้ได้จริง: **TikTok Creative Center** (creativecenter.tiktok.com — filter ไทย + Fashion Accessories) + **Google Trends** · Shopee/Lazada Seller Center เป็นแค่ analytics ร้านตัวเอง ไม่ใช่เครื่องมือส่องตลาด · สรุป: เป็นรูทีนคนเช็คเอง ระบบแค่ต้องมีที่ให้จดผลไว้ | ⬜ รอ Luffy ตัดสินใจว่าจะทำเป็นรูทีนคน หรือแค่จดในระบบ |
| F3 | 🎬 คอนเทนต์/วิดีโอ — เช็คเทรนด์/ไวรัลในไทย | เครื่องมือเดียวกับ F2 (TikTok Creative Center ตอบได้ทั้งคู่) · ซ้อนกับ **D3** (planner คอนเทนต์ 14 คลิป/สัปดาห์) — ควรรวมเป็นงานเดียวกัน | ⬜ รวมกับ D3 |
| F4 | 🗃️ คิวสินค้าค้าง (Backlog Board) | **🔒 ขอบเขต (เคาะแน่นอนแล้ว 19 ก.ค. รอบ 3): จบที่ Timeline ข้อ 4 "ส่งบัญชี" เท่านั้น** — ห้ามลากยาวไปถึงถ่ายรูป/ลงช่องทาง/คอนเทนต์/วัดผล (ดูตาราง Flow งานเต็มด้านบน) พอ SKU มีต้นทุน+ราคา+ส่งบัญชีแล้ว ออกจาก Backlog ไปเลย ไปอยู่ในคัมภีร์สินค้าตามปกติ · **ปัญหาเดิม:** สินค้า 10-20 อัลบั้มที่ซื้อมาก่อนมี Scout ข้อมูลกระจายไลน์/กระดาษ/สมุด ยังไม่เข้าระบบ + ของใหม่จาก Scout (กด Yes→ซื้อแล้ว→"นำเข้า Backlog") ก็มารวมกันที่นี่เหมือนกัน — 2 ทางเข้า ปลายทางเดียว · ไอคอน 🗃️ (ไม่ใช้ 📦 กันซ้ำกับ In Non-RB) · **🔀 Flow ที่เคาะแล้ว (19 ก.ค. รอบ 3 — แทนที่ดีไซน์กริดเดิมทั้งหมด):** (1) Luffy มอบหมาย SKU ในกองค้างให้ user คนใดคนหนึ่ง (2) user นั้นกรอกเฉพาะฟิลด์บรรยาย **ไม่มีต้นทุน/ราคาเลย**: รูป, หมวด, Class, RBO SKU, ชื่ออัลบั้ม, ชื่อตัวเลือก, รหัส RBWS, น้ำหนัก, Location RBO, Location RBWS, รหัสสี RBWS (ตรงกับฟิลด์ฟอร์ม ➕ Add SKU เดิมเป๊ะ ยกเว้นทุน/ราคา) แล้วกด Save ทีละ SKU (3) Luffy/Assist มารีวิวทีละ SKU ที่กรอกครบ ผ่านแล้วเลือกเส้นทาง **"➕ SKU (บันทึก SKU ใหม่)"** หรือ **"🛍 In Non-RB"** (4) ไปต่อ pipeline บัญชี/ต้นทุนที่มีอยู่แล้วในฟอร์มนั้นๆ ตามปกติ — จบ Backlog Board ตรงนี้ · **มอบหมายงาน:** `assigned_to` ดึงรายชื่อจากชีต Users เดิม (luffy/assist/admina/adminb/graphic/store) — ยังไม่เคาะว่า user ที่ถูกมอบหมายจะเห็นหน้าไหน/กรองยังไง (ถามแล้ว Luffy ยังไม่ตัดสินใจ) · **⛔ กฎต้นทุน (ยืนยันซ้ำ):** ต้นทุนเห็น/แก้ได้เฉพาะ Luffy/Assist เท่านั้น ไม่ว่าจะมอบหมายให้ใคร ต้นทุนใส่ทีหลังตอน Luffy/Assist กด "➕ SKU"/"🛍 In Non-RB" เอง (ฟอร์มปลายทางมีเครื่องมือคำนวณราคาของตัวเองอยู่แล้ว ไม่ต้องสร้างซ้ำใน Backlog) · **❌ ไอเดียที่เสนอแล้วตัดทิ้ง:** เคยเสนอให้เช็ค "ช่องทางการจำหน่าย" (web_status) ตอนรีวิวก่อนส่งต่อ — Luffy บอกงงและสั่ง ignore ไปแล้ว (19 ก.ค.) **อย่าเสนอซ้ำโดยไม่ถามใหม่** | ✅ **B3.108 (19 ก.ค.): สร้างใหม่ทั้งหมดตาม flow รอบ 3 แล้ว push แล้ว** — 4 แท็บ: **📊 ภาพรวม** (กลุ่มตามอัลบั้ม โชว์กรอกอยู่/รอรีวิว/ส่งต่อแล้ว) · **➕ เพิ่มรายการ** (กริดสั้น: อัลบั้ม/ชื่อตัวเลือก/มอบหมาย เท่านั้น ไม่มีทุน/ราคา) · **📝 กรอกรายละเอียด** (การ์ดต่อ SKU กด ✏️ เปิด modal กรอกครบ 11 ช่อง: รูป/หมวด/Class/RBO SKU/อัลบั้ม/ตัวเลือก/RBWS/น้ำหนัก/Location RBO/Location RBWS/สี RBWS — เช็คครบอัตโนมัติ ครบเมื่อไหร่ขยับสถานะเป็น "รอรีวิว" เอง) · **✅ รอรีวิว** (เฉพาะ L/A — เห็นข้อมูลครบของ SKU นั้น กดปุ่ม "➕ ส่งไป SKU ใหม่" หรือ "🛍 ส่งไป In Non-RB" mock ไว้ก่อน) · Scout "นำเข้า Backlog" ปรับ mapping ใหม่แล้ว (เข้าเป็น draft รอกรอกรายละเอียด ไม่พกทุน/ราคาติดมาด้วย) · ทดสอบผ่านเบราว์เซอร์จริงครบ: เพิ่มรายการ→กรอกไม่ครบยังเป็น draft→กรอกครบขยับเป็น ready อัตโนมัติ→รีวิว→ route→overview นับถูก, non-L/A ถูกกันไม่ให้เข้ารีวิวได้, Scout import ได้ shape ใหม่ถูกต้อง · **~~ค้าง: ยังไม่มี backend จริง~~ → ✅ B3.111-B3.112/3.97 (20 ก.ค.): ปุ่ม route เปิดฟอร์มจริง+เติมข้อมูล+รูปแล้ว · backend ชีต Backlog + backlogList/Add/Update/Route เขียนเสร็จ (ดูสรุปจบงาน 20 ก.ค. ด้านบน — รอ Luffy deploy)** · **✅ เคาะแล้ว 19 ก.ค. (รอบ 4): เปิด nav Backlog ให้ทุก role เห็นแล้ว** (ไม่มีต้นทุนอยู่ใน Backlog เลยจึงไม่มีอะไรต้องซ่อน) — พนักงานที่ถูกมอบหมายเข้าแท็บ 📝 กรอกรายละเอียด ได้จริงแล้ว ส่วนแท็บ ✅ รอรีวิว ยังกันด้วย `isLA()` เฉพาะ Luffy/Assist เหมือนเดิม (ทดสอบผ่านเบราว์เซอร์จริงแล้วด้วย role graphic: เห็น Backlog, เพิ่ม/กรอกรายการได้, เข้ารีวิวไม่ได้ — Scout ยังคงซ่อนสำหรับ non-L/A เหมือนเดิมไม่เปลี่ยน) · **🐛 บั๊กที่เจอ+แก้แล้ว (19 ก.ค. รอบ 5, build 3.96/3.96):** ดรอปดาวน์ "มอบหมายให้" เคย hardcode รายชื่อ (Luffy/Assist Manager/Admin A/Admin B/Graphic/User หน้าร้าน WS) ตรงๆ ใน JS — Luffy แก้ชื่อจริงในชีต Users แล้ว (เช่น Admin A → ชื่อพนักงานจริง) แต่ระบบไม่อัปเดตตาม เพราะไม่เคยดึงจากชีตจริงเลย → **แก้แล้ว:** ดึงชื่อจริงผ่าน action `userList` (เดิม master เท่านั้นเรียกได้ ขยายให้ Assist เรียกได้ด้วย) โหลดทุกครั้งที่เปิดแท็บ Backlog (เฉพาะ L/A — role อื่นเห็นแค่ placeholder "ยังไม่มอบหมาย" เพราะ userList ยังจำกัด L/A) ทดสอบยืนยันแล้วว่าเปลี่ยนชื่อในชีต Users แล้วโชว์ถูกต้องในดรอปดาวน์ · **✅ deploy ขึ้นเว็บจริงแล้ว** (clasp push + clasp deploy ไปที่ deployment เดิม @110 ยืนยันเว็บจริงตอบ build 3.96 แล้วด้วย curl) |

---

## 🩷💬 เฟส E — Automation (ดองไว้ รอ routine นิ่ง ตามที่ Luffy สั่ง)
| # | โมดูล | สถานะ |
|---|---|---|
| E1 | 💬 LINE 5 กลุ่ม + Escalation (ค้าง1วัน→เตือนคนทำ · 3วัน→กลุ่มบริหาร) · **เคาะแล้ว 15 ก.ค.: ใช้ LINE OA แพ็กฟรี (300 ข้อความ/เดือน พอสำหรับ ~100) · LINE Notify ปิดบริการแล้ว ต้องใช้ Messaging API · รอ Luffy สมัคร OA + ส่ง Channel Access Token · **แผนเคาะ 16 ก.ค.: reply ฟรีไม่จำกัด→ทำถามตอบได้เต็มที่ · เริ่ม 3 กลุ่ม (เบิก-สต็อก / ปฏิบัติการ RBO / บริหาร) · Low Stock ส่ง digest วันละฉบับ · ตัวนับโควต้าหยุดที่ 280 · ลำดับ L1 โครง+digest → L2 ถามตอบ → L3 สรุปเช้า+escalation → L4 บัญชี/คอนเทนต์+ขาเข้า** | 🔨 L1 เสร็จ B3.84: webhook จับกลุ่ม→LineGroups · เลือกกลุ่มรับแจ้งใน Setting · lineSend_+โควต้าหยุด 280 · digest **14:30 เฉพาะจันทร์–ศุกร์** — **3.91 (17 ก.ค.): พบ trigger 8:30 เก่าค้าง (เด้งผิดเวลา) → setupLineTriggers ลบเก่าทุกตัว+ตั้งใหม่ inTimezone Asia/Bangkok+รายงาน trigger คงเหลือ · เช็ค จ–ศ ในตัว lineDailyDigest_ เอง (trigger ซ้ำ/ผิดยังไงวันหยุดก็เงียบ) · ⚠️ ต้องรัน setupLineTriggers ซ้ำ 1 ครั้งหลัง deploy 3.91** · reply ฟรี · B3.88 แก้โควต้าไม่นับ (คีย์เดือนโดน Date coercion → เติม M นำหน้า) · B3.86: ติ๊กหน้าที่รายกลุ่ม use_stock/use_wd/use_exec/use_content (🎨 คอนเทนต์/กราฟฟิค เพิ่ม 16 ก.ค.) (chips ใน Setting) · digest ส่งทุกกลุ่มที่ติ๊ก 🔔 · จับกลุ่มสำเร็จแล้ว 16 ก.ค. · **L2 เสร็จ B3.89: ถามตอบในกลุ่ม เช็ค<คำค้น>/เบิก/ช่วย (reply ฟรี · cost ไม่ตอบ · ข้อความอื่นเงียบ)** → ต่อ L3 สรุปเช้า+escalation |
| E2 | 🩷 คอนเทนต์+AI + n8n ลงโพสต์อัตโนมัติ | ⏸ |

## 📊 กองแยก — ไม่ใช่โค้ดเว็บ
| # | งาน | สถานะ |
|---|---|---|
| X1 | วิเคราะห์ยอดขาย Shopee (ไฟล์ Order+Income ม.ค.–พ.ค. 69 อยู่ใน project แล้ว 10 ไฟล์) | ⬜ รอสั่ง |
| X2 | LJ sub-brand (แพ็กเกจหรู Moissanite) | ⬜ |
| X3 | Tagline Rainbow Online ("wear your vibe" ยังไม่เคาะ) | ⬜ |

---

## ลำดับที่ตกลง: A → B → C → D → E
เหตุผล: A เร็วเคลียร์หนี้เก่า · B คือของที่ธุรกิจต้องใช้จริงก่อนออกแฟร์ · C ต่อยอด B (สต๊อกแฟร์ต้องนิ่งก่อน sync) · D อิสระ แทรกได้ · E รอ routine นิ่ง

## ⚠️ กติกาทำงาน (ห้ามลืม)
- BUILD stamp 3 จุดตรงกัน (BUILD / FRONT_VER / doGet) ก่อนส่งทุกครั้ง
- `node --check` ผ่านก่อนส่ง + เช็คฟังก์ชันซ้ำ = 1 ทุกตัว
- ส่งไฟล์พร้อม snippet เช็คเวอร์ชัน F12
- แก้ผ่าน Python · อ่านโค้ดเดิมก่อนแก้ · cp แยกคำสั่ง ไม่ chain &&
- Deploy: Edit deployment → New version เท่านั้น · HTML อัพผ่าน GitHub Upload files
- CSV บัญชี: คอลัมน์ล็อกถาวร ห้ามมี comma ในตัวเลข
- เปิดเว็บจริง https://luffy108269.github.io/... เท่านั้น (ห้ามเปิด file:// — เจอบทเรียนแล้ว 15 ก.ค.)
- **ไฟล์ frontend ส่งชื่อ `index.html` เสมอ — ห้ามใช้ index_build2.html (Luffy อัพขึ้น GitHub Pages ตรงๆ ไม่ต้องแก้ชื่อ · กำหนด 17 ก.ค.)**
